# 0.10.0

- add `tokio_local!` to make env module working with `Context`
- add `quickjs` runtime in env module
- use `quickjs` runtime in `pack` instead of `wit`
- remove `start` function from `Engine`
- add `Builder` to build engine with different config
- add workflow `env` to support all workflows can get env vars and set locally
- simplified the options of the `error` action
- merge action state to task state
- add engine channel to receive messages by options and the channel messages can re-send if not acked

# 0.10.1

- remove the warning code
- fix the doc test error
- rename engine.emitter to engine.channel
- rename data::message emit_id to chan_id, emit_pattern to chan_pattern
- delete data::message emit_count
- remove default feature

# 0.10.2

- update readme.md
- add homepage

# 0.10.3

- remove action result, the time will caculate by acts-channel
- refactor the info struct to make is easier to understand.

# 0.10.4

- modify the test error with 'store' feature

# 0.10.5

- remove the warnings in rust 1.82
- remove the duckdb bundle feature

# 0.10.6

- reset the build mode to bundled for store feature

# 0.11.0

- change store db to sqlite

# 0.12.0

- change the act yml format, use act: xx instead of !xx
- add setup to act and remove on_created, on_completed
- add act.expose for pack
- add nid for Message
- use 'do' act instead of 'cmd'
- expands executor with msg(), pack(), proc(), task(), act() and mode() instead of manager

# 0.12.1

- update act.set_output to act.expose
- keep act.expose only expose the vars to outputs
- fix the model tree output issue

# 0.12.3

- fix the test error with feature store

# 0.12.4

- fix test error for act each result check issue

# 0.12.5

- add export.msg unsub to support unsubscribe the messages by client
- fix the deadlock issue by subscribing with same client id by many times

# 0.13.0

- change the the query function to return PageData in trait DbSet for store collection
- add query_by and order_by to query fn
- add `mid` to message collection
- add ExecutorQuery to export list fn for msg, pack, proc, task, message

# 0.13.1

- fix: fix the memory store query error

# 0.13.2

- upgrate rquickjs to 0.8.1

# 0.13.3

- merge Luminvent's change
- fix: fix the clippy error

# 0.14.0

- improve the code quality
- add set_process_var for act_execution

# 0.15.0

- feat: allow to keep processes after completion
- fix: set process state if task is completed and is root task [#12](https://github.com/yaojianpin/acts/issues/12)

# 0.16.0

- feat: reafactoring the act package to support act extension [#8]
- feat: adding package meta struct to support package jsonschema definition [#9]
- feat: add acts-sqlite plugin
- feat: add acts-postgres plugin #[13]
- feat: add acts.cfg support
- feat: modify Config to support getting custom config section
- feat: add workflow.on events (manual, hook, chat)
- feat: add acts-state package to support get or set state
- feat: change the directory structure with acts, plugins, examples, benches, config

# 0.17.0
- feat: reactoring env module to add register_var trait
- feat: add "resects" user var to support get resects data from task context
- feat: add step env module to support get step vars by step id
- feat: change env var to $env
- feat: rename proc env_local to env
- feat: change event package params to Option<T>
- feat: skip initialization of plugin when there is no related section
- feat: add `acts.core.http` package plugin
- feat: add `pid` prefix to the acts.app.state package
- feat: support private vars (starts with `__`)
- feat: use `acts.toml` instead of `acts.cfg`
- feat: add `env.expose` in workflow to support set default outputs, the default outputs is `data`
- feat: add `acts.app.shell` package plugin to support nushell, bash and powershell
        support {{ }} refs the var in shell script
- feat: add `os` var in expression
- feat: use {{ }} for expression instead of ${ }
- feat: use var name directly in script or expression instead of $("var")
        use $get("var") instead of $("var")
        use $set("var", value) instead of $("var", value)
        use $inputs() instead of $act.inputs()
        use $data() instead of $act.data()

# 0.17.1
- fix: fix examples/plugins build issue when exclude examples

# 0.17.2
- fix: add info.MessageInfo uses property

# 0.18.0
- feat: change the workspace structure
- feat: use Query instead of ExecuteQuery
- feat: add `match` operation to FilterExpr
- feat: add `ui_schema` property to `ActPackageMeta`
- feat: add `vars` property to `Workflow`, `Step` and `Act`
- feat: change `inputs` and `outputs` to Json schema definitions of workflow
- feat: add `metadata` property to store UI data
- feat: remove `setup` property of `Workflow`, `Step` and `Act`
- feat: add `start_from_model` to export.proc
- feat: move `message.model` to `message.inputs.model`
- feat: refactoring `catches` and `timeouts` for `step` and `act`
- feat: change `vars`, `env` and `options.exposes` to `Vec<Variant>`
- feat: change `workflow.inputs` and `workflow.outputs` to `ActSchema`
- fix: fix the audit issues
       1. fix dependencies sqlx-mysql `rsa` issue （Timing Side-Channel Attack）
       2. fix dependencies rquickjs `dlopen_derive` `unmaintained` issue
- feat: add `rn` resource name to workflow and step
- feat: update workflow.ver to String type
- feat: add `desc` to store models
- feat: add dashmap for Package mod
- feat: add safe limit for quickjs runtime
- feat: change db store to kv store
        add db with sqlite, postgres, nats, redis, sled

# 0.19.0
- feat: remove `acts` from step struct
- feat: add `uses` and `params` to step struct
- feat: remove `catches` and `timeouts` from act struct
- feat: change `catches` and `timeouts` list from `Vec<Act>` to `Vec<Step>` in step 
- feat: remove `key` from message struct, the `key` info should be migrate to `params` properties
- feat: add document for english and chinese language
- feat: add default workflow version
- fix: fix issue #16 not save the data after setting the process vars

# 0.19.1
- fix: fix sch_task_start and pack_parallel_setup_list run fail

# 0.20.0
- remove `tag` and `rn` from workflow, step, branch and act
- use `options` to support the user custom extension, such as `tag`, `rn`
- add `exposes` to workflow, step, branch and act instead of options.exposes
- change `timeouts` and `catches` to support complex flow like `steps`
- add `Engine::builder()` to `Engine` and remove the export of EngineBuilder
- merge `ActPackageFn`  into into `ActPackage`
- change expression `{{ }}` to `${{ }}`
- merge the task logic `review` to `next`
- feat: add uses action check in  `task.next` before run into children tasks

# 0.20.1
- fix the package publish version issue closes #17

# 0.21.0
- migrates `acts-channel` from https://github.com/yaojianpin/acts-channel
- add plugin `acts-plugin-grpc` and `acts-plugin-web`
- add resolver to `EngineBuilder` and `Engine`
- feat: `console.log` supports multiple parameters
- feat: add `acts-package-nats` pubsub package
- feat: modify `execute` of `ActPackage` to async
- feat: change `ActTask` `run` to async
- feat: change `cache.upsert` to `cache.upsert_async` to improve performance
- perf: move `task.next` to async queue
- perf: optimize in-memory scan_prefix performance.
- perf: optimize `convert::get_expr` and `convert::get_exprs` to improve `fill_params` performance
- perf: optimize list iter and add shutdown token to runtime
- fix: act data cannot update to parent when abort or error task, test cases #pack_action_abort_on_step_with_inputs, pack_action_error_on_step_with_inputs
- feat: add tracing log for key function
- fix: fix examples/simple exposes type error

# 0.21.1
- fix: fix acts-channel version dependency issue, close #18

# 0.22.0
- change rust ci branch to `main` and `develep`
- remove truncate data issue in store/postgres
- feat: `store-*` features only control compilation; the matching store struct is exported when enabled
- feat: select the store with `EngineBuilder::set_store(Arc<dyn KvStore>)`, only one store is allowed, default is in-memory
- BREAKING: remove `Extender::register_store` and the runtime store override
- BREAKING: remove the `db` config section (`DbConfig`); create the backend struct externally and pass it to `set_store`
- fix: fix the event delivered to handlers in FIFO order
- feat: add `set_config` to `EngineBuilder` and unit test
- fix: change task id format with `{pid}{tid}`, remove `SEP` between `pid` and `tid`
- fix: add QuickJS interrupt handle to deal with deadline issue
- feat: change PostgresStore connection from global to local
- fix: make `task.next` propagation crash-safe with a durable outbox — pending `next` operations are recorded in a new `ops` store collection before in-memory dispatch and closed only after the task (with the `NEXT_COMPLETE` marker) is durably persisted; recovery replays unfinished records, and re-scheduling is deduplicated, so reloading after a crash never loses or duplicates propagation (removes the non-durable `NEXT_PENDING` sign)
- fix: make `task.action` propagation crash-safe with a durable outbox
- fix: fix the hang issue when executing `pack_irq_multi_threads`
- fix: bound the re-execution of a tree node per process — add `max_node_run_times` (default 1000, `EngineBuilder::max_node_run_times`, 0 disables); a node `next` self-loop or cycle now errors the process instead of creating tasks forever
- fix: `process.do_tick` fires timeouts only for tasks still in flight — completed, error, aborted and skipped tasks no longer schedule their timeout children on every tick
- fix: keep `TaskTree.push` only insert and not replace old task
- fix: one-shot atomic start process
- fix: not return error when tree build error
- fix: add `limit`, `ExprOp::Between`，`ExprOp::In` validation in store query
- fix: `StoreWriter.close` flushes, stops and joins the writer thread — when it returns no writer thread is left running, and later `send`/`flush` calls fail with a channel-closed error (close previously returned without joining the thread)
- fix: store writer `flush` reports the first failure of the writes queued since the previous flush — a failing write is surfaced through the barrier instead of acksing `Ok`; `Cache::flush` returns `Result`
- fix: serialize process removal through the store writer (`RemoveProc` op) — `cache.remove` no longer deletes the process rows directly while its completion markers are still queued, so removal can never race the pending writes; task writes reaching the writer after the removal are skipped instead of resurrecting rows or erroring
- fix: store index range scans were off by one at the exact value boundary — `Between`/`Le` dropped `value == to` and `Gt` included it — because index keys embed the record id after the value, so no key string can bound a whole value group; range/inequality ops now translate to half-open full-key intervals closed by a sentinel bound (`KEY_SEP_SUCC`), making inclusive/degenerate ranges and single-sided comparisons exact on every backend
- fix: `-` is `KEY_SEP` yet passed through the index value encoding, so `Eq`/`Ne` on `x` also matched stored values like `x-2`, and hyphenated strings leaked across range bounds by id order; `-` is now escaped (`=2D`) in value segments
- fix: string range/inequality queries on indexed fields fall back to the exact full-data scan (escaped characters do not sort in code-point order), keeping results correct; numeric fields keep the exact index path
- feat: add `Store::rebuild_indexes` / `KvCollection::rebuild_index` to recreate index entries after the encoding change — run once when upgrading existing stores (old hyphen-encoded index keys are dropped)
- fix: store `query` with `order_by` now sorts the whole matching set BEFORE pagination, so every page is the global top-N slice instead of a re-sorted arbitrary batch; multiple `order_by` fields apply in listed priority with per-field `asc`/`desc`; numbers sort numerically (previously compared as text, so `10` < `9`); a document missing or `null` on a sort key no longer panics the comparator — it sorts first on `asc` (last on `desc`), and rows tied on every key fall back to ascending id so offsets stay deterministic across pages
- feat: steps can now be written as bounded `while` loops — add `while: <cond>` and the step re-executes while the condition holds, then falls through to the next declared step when it fails (no self-`next` needed; `while` cannot be combined with `next`)
- fix: a step’s explicit `next` jump is preserved at model build — the following step in declaration order no longer clobbers it, and a step skipped by its `if`/`while` condition falls through to the next declared step instead of re-entering a self/backward `next`; the `max_node_run_times` guard still errors true (unconditional) cycles
- fix: stop sending the message if message retry update fails
- fix: make `Signal` one-shot fires broadcast — a single `send`/`close` now releases every concurrent `recv` waiter and receivers joining after the fire return immediately; previously `Notify::notify_one` woke at most one receiver, so the rest hung forever (`update` closures may still call `close` on the same signal)
- refactor: replace `std::sync::{Mutex, RwLock}` with `parking_lot` everywhere — locks are infallible (no `unwrap`/`map_err` at call sites) and guards are smaller; `tokio::sync` stays for async-held locks
- fix: stop sending message if `store_if` returns `false`
- BREAKING: messages are now stored split: the canonical emitted `Message` (one row per message id, payload stored once) in the `messages` collection, and one `Delivery` row per (message × channel/service) in a new `deliveries` collection — Ack/Retry/Clear/Redo, the retry timer and task-completion close-outs all operate on delivery rows keyed by their own delivery id; every channel delivery of the same event shares one `msg_id` but has its own `delivery_id`, so multiple grpc/SSE clients (and future nats/kafka adapters) ack independently
- feat: each delivered event carries `delivery_id`; channel handlers store the canonical message once and create one delivery row per ack channel, tagging the handler event with the new delivery id so the client can ack exactly its own delivery
- feat: retry re-sends are routed to the owning channel only (`Emitter::emit_delivery`) — an acked channel never sees another channel's retries; each redelivery reuses the same delivery id for exactly-once consumer dedup
- feat: message manager ops — `msg:ack`, `msg:get`, `msg:rm` key on delivery ids; `msg:clear`/`msg:redo` accept an `id` for one delivery while keeping the batch `pid`/all forms (`MessageExecutor::clear_delivery`, `redeliver`)
- feat: delivery rows expose `msg_id`/`chan_id` indexes; `MessageInfo` joins delivery state with its canonical message
- migration: existing v1 merged message rows read as canonical messages (delivery state is not carried over); run `Store::rebuild_indexes` once when upgrading existing stores
- BREAKING: `Workflow.on` entries are now triggers (`kind` + `params`), not `Act`s — replace `uses: acts.event.manual|hook|chat` with `kind: manual|chat|hook`. Triggers only declare the workflow start surface; `Act` stays for in-process steps/actions
- feat: add `Trigger` model (`kind`: `manual`/`chat`/`hook`/`schedule`, or a registered event package id for custom triggers); `EventInfo` exposes `kind` + schedule run state
- feat: `schedule` triggers — engine timer polls due cron rows (`sec min hour day month dow`) and starts the workflow; run state (`last_run`/`next_run`) is persisted on the trigger row, survives restarts, and manual firing is refused
- feat: web URL triggers — a `manual` trigger fired over HTTP doubles as a webhook; `acts-plugin-web` adds `POST /hooks/{model-id}:{trigger-id}` that starts the declared trigger with the request body as payload and returns the process id (no separate `webhook` kind)
- fix: model re-deploy now reconciles trigger rows — changed triggers update, removed triggers are deleted (previously stale rows stayed fireable); trigger rows keep their schedule state across re-deploys unless the cron changed
- BREAKING: remove the `acts.event.manual|hook|chat` packages (superseded by trigger kinds)
- migration: existing v0 `events` rows (`uses: acts.event.X`) are upcast to `kind` on load
- fix: collection writes are atomic per document — `KvStore` gains `batch` (applies a `StoreBatchOp` list all-or-nothing via a native transaction on memory/sled/sqlite/postgres/redis, sequential best-effort on nats), and `DbCollection::create`/`update`/`delete` now commit the data row and its index rows in one batch, so a mid-write failure can no longer leave a document without its indexes (or index rows without the data row)
- fix: model `deploy` is atomic across the model row and its trigger (`events`) rows — trigger reconciliation (create/update/stale-drop, schedule state preservation) moved from `ModelExecutor` into `Store::deploy` and commits with the model row in one `KvStore::batch`, so a mid-deploy failure can no longer leave a deployed model with half-reconciled triggers
- fix: model removal is atomic too — `ModelExecutor::rm` now deletes the model row and its trigger (`events`) rows in one `KvStore::batch` (moved into `Store::rm_model`), so a mid-removal failure can no longer leave stale trigger rows behind
- fix: process removal is atomic — `Store::remove_proc` deletes the process's task rows, durable outbox (`ops`) rows and the proc row in one `KvStore::batch` (`remove_proc_rows`), so a crash mid-removal can no longer leave a half-deleted process that would resurrect broken on restore
- fix: a process's first persist is atomic — `Process::start` now writes the proc row and its root task row as one `KvStore::batch` (`Cache::start_proc` → `Store::upsert_proc_with_task`) before the root task is dispatched, closing the crash window where a durable Running proc row had no task rows yet and would resume as an un-runnable, task-less process
- fix: `Engine::start` failure now releases the partially started runtime — the store writer thread, event loop, recovery writes and any timer tasks are torn down (`rt.close()`) before returning the error, instead of leaking a live writer std-thread and polling timers on an engine that never became usable
- BREAKING: `EventExecutor::start` (and its `start_hook` helper) is now `async` — the `hook` trigger awaits the completion signal directly (`sig.recv().await`) instead of parking a `sync::block_on`; callers of `executor.evt().start(...)` must `.await` it
- BREAKING: change `ActTask::init` to `async`
- BREAKING: `KvStore` / `DbCollection` are now `async` traits (`#[async_trait]`; native `async fn` cannot be dyn-dispatched) — implementers of custom stores must make every method `async` and add `#[async_trait::async_trait]`
- BREAKING: store backends open asynchronously — `SqliteStore::open`/`open_in_memory`, `PostgresStore::open`, `NatsStore::open`, `RedisStore::open` are now `async` (sqlite keeps one connection behind a `tokio` mutex; redis moves to the async multiplexed client); `MemoryStore`/`SledStore` are unchanged
- BREAKING: the whole store façade is async — `Store` (incl. the `cache` writer extensions), `KvCollection`/`DbCollection`, `Cache` (`proc`/`remove`/`restore`/`flush`/`close`/…), and the scheduler event/timer/recovery paths await directly; `utils::sync::block_on` and the dedicated second tokio runtime are removed, so store operations no longer panic on current-thread runtimes or steal worker/blocking threads on multi-thread runtimes
- BREAKING: the store writer is a tokio task instead of a std thread — FIFO ordering and flush-barrier semantics are unchanged, but `StoreWriter::flush`/`close` (and `Cache::flush`/`close`, `Engine::close`) are `async`
- BREAKING: `Engine::start` and `Engine::close` are now `async` (start recovers pending actions and drains the writer on failure; close drains and joins the writer task)
- BREAKING: all executor data methods are `async` — `model().deploy/list/get/rm`, `proc().start/start_from_model/list/get/get_process`, `act().submit/back/cancel/complete/abort/skip/fail/push/remove/set_process_vars/do_action`, `msg().list/get/ack/rm/clear/redo/clear_delivery/redeliver/unsub`, `task().list/get`, `pack().publish/list/get/rm`, `evt().list/get`; registration/accessor methods stay sync
- BREAKING: channel and emitter handlers are now async closures — `on_message/on_start/on_complete/on_error/on_proc/on_task(|e| async move { … })` with the event owned by value; events of one process are delivered in emission order (never concurrent), while different processes run concurrently — a slow handler only stalls its own process
- BREAKING: `Extender::register_package`, `package::init`, `ActPackage::start` and `ActPackageDefinition` publishing are async (a plugin that published packages from `ActPlugin::on_init` must publish them from an async context instead)
- BREAKING: `SqliteStore` no longer exposes the `conn` field; `RedisStore` no longer exposes a synchronous `redis::Connection`
- perf: store operations are true awaits on the ambient runtime — no `block_in_place`, no per-op thread hop, no second runtime; PG/NATS round trips no longer pin threads, redis no longer blocks a worker inline
- fix: engine store ops work under current-thread `#[tokio::main]`/`#[tokio::test]` runtimes (previously any async-context store op with a real backend panicked on `block_in_place`)
- BREAKING: remove the `keep_processes` config option — a finished process is always cleaned up (its proc/task/outbox/message/delivery rows are deleted once its deliveries have settled); there is no "keep completed processes" mode anymore
- BREAKING: the delivery row status type is now its own `DeliveryStatus` (renamed from `MessageStatus`; same integer codes, zero DB migration) with an explicit lifecycle — `Created` (row stored, not yet successfully handed over) → `Delivered` (delivery succeeded: the channel handler ran to completion; marked only from `Created`, never downgrading a row the handler acked/closed) → `Acked` (client confirmed, intermediate) → `Completed` (engine close, final) — `Error` (retries exhausted) is resolved only by a manual resend/clear and keeps its process alive until then
- feat: process cleanup is now driven by delivery settlement instead of the terminal state — a finished process is not deleted synchronously; when a task reaches a terminal state the engine-authoritative writer closes the task's own delivery rows (`Completed`) and re-checks the proc's `removable` mark (a process with no delivery rows — every message done with its own state — or all deliveries settled is marked on the spot); the retry-timer sweep then deletes marked processes through the writer (`RemoveProc`, FIFO after any still-queued writes) in one atomic batch with their message/delivery/outbox/task rows, so an `Error` delivery (manual handling) or still-settling rows can never race the removal
- fix: index scans for `In`/`Ne`/range/`Between` recovered the record id with the LAST `-` separator — an id containing `-` (e.g. a user-supplied process id) was truncated and the row silently dropped from results; the value segment can never contain `-` (escaped), so the FIRST separator is the value/id boundary
- fix: a late `msg:ack` no longer downgrades a delivery the engine already closed — `Completed` is final and a stale ack is a no-op (previously the ack reverted the row to the intermediate `Acked` and could un-settle the process)
- fix: `msg:rm`/manager views aside, deliveries of a finished process are removed with it — unacked `Created`/`Delivered` rows of a dead process are no longer re-sent by the retry timer (deleting the process stops the retries)
- feat: `exposes`/`inputs`/`vars` entries whose `type` is omitted are no longer treated as `string` — the exported value is validated and kept by its runtime JSON type (previously a name-only `exposes` entry exported as `string`, so a number/boolean/object output failed completion with a type error); a literal `value` derives its concrete type (e.g. `value: 10` ⇒ `number`); an explicit `type` still validates strictly, and serializing a model no longer writes back an implicit `type: string` (round-trips preserve the untyped state)
- feat: task scope vars are decoupled from the task lifecycle row — a new `vars` collection (`data::TaskVars`, keyed by the same composite `pid + tid` as its task) holds each scope's `data`/`sealed`; a task persist now writes the small lifecycle row and only the vars rows whose scope data actually changed (mutation-tracked dirty flags on the task, flushed over the parent chain by both the sync and the writer persist paths), so pure state transitions no longer rewrite the root row on every task write, and a child output folding up to an intermediate declaring ancestor is persisted at that owner — previously only the root was re-written, leaving intermediate ancestor scopes stale after a crash
- feat: restore loads task vars with one pid-scoped `vars` query and re-attaches them onto the reloaded tasks; durable-row manager views (`task().list`/`task().get`) join the paired vars row for `TaskInfo.data`
- migration: task rows written before this change still parse (their embedded `data`/`sealed` are ignored); scope vars of in-flight processes are not carried over to `vars` rows — a restart of the engine re-persists current scopes on their next flush
- test: cross-backend `store_task_vars_*` cases (create/find, pid-scoped + tid index queries, update without row duplication, remove with index cleanup, upcast) plus cache cases proving lifecycle-only writes never touch vars rows and an ancestor-scope update survives a store round-trip without writing the root
- fix: evict a finished process from the in-memory cache on its terminal proc event — its durable rows stay (the sweeper removes them once every delivery settled), but the freed slot lets the `restore` pass pull other persisted processes (e.g. left over from a crash) back into the cache and auto-start them; previously finished processes squatted in the cache at/over the restore checkpoint and blocked restoring others, and restore passes (triggered by terminal events of different processes, which run concurrently) are now serialized so two passes can never load and auto-start the same persisted process twice
- BREAKING: the in-memory proc cache no longer evicts — moka's LRU cache is replaced by an exact resident map whose capacity (`cache_cap`) is enforced at start time instead of by cache pressure: when the resident set is full a new process is *parked* (its durable row is persisted with state `None`, it is not resident) and the restore pass after the next terminal event starts it, oldest first; previously an over-cap start evicted an arbitrary resident process — usually a long-running or client-waiting one, since recency of cache access is unrelated to liveness — which orphaned the pid (its queue tasks and tick loop keep the old instance alive) and reloading it later created a second `Process` instance racing the first. Running processes are now never evicted, so a live pid always has exactly one in-memory instance
- perf: `restore` refills free slots from parked rows with an indexed `state = none` query ordered by creation (oldest first), replacing the checkpointed rescan of the whole non-terminal set — a slot is topped back to `cap` on every terminal event, and parked demand is bounded, not queued in memory
- fix: a process parked over `cap` is no longer cached by an on-demand `proc()` lookup (a resident parked row would occupy a slot forever, since restore skips resident pids and would never start it)
- feat: engine restart now resumes processes that were in flight when it crashed — boot loads durable `Ready`/`Running`/`Pending` proc rows into the resident set (oldest first, ahead of parked `None` rows, capped) and re-dispatches the tasks the outbox replay left mid-flight; the durable outbox records are replayed first (`recover_actions`), then only tasks with NO pending record are re-run through `exec` — a `Running` leaf cut off mid-run is reset to `Ready` and re-executes (at-least-once: a crash-point task may produce its external effect once more, converging through the state-machine guards `NEXT_COMPLETE`/`IN_CHILDREN`/`schedule_once` dedup), while running parents are driven by their resumed children and `Pending`/`Interrupt` tasks wait for their parent/client as before; previously a mid-run task whose `run` was interrupted (no outbox record yet) stalled forever after a restart — only op-driven propagation was recovered
- fix: boot-resume overflow is no longer stranded — in-flight (`Ready`/`Running`/`Pending`) rows beyond the resident cap are queued at boot (oldest first) and each slot freed by a terminal event now resumes one of them (`Runtime::restore` → `Cache::resume_from_queue`), since `restore` only refills parked (`None`) rows; previously the overflow was only warned about and those processes waited forever


# 0.23.0
- BREAKING: the external store backends moved out of `acts` into the new `acts-store` crate — `SqliteStore`, `PostgresStore`, `RedisStore`, `NatsStore` and `SledStore` are no longer exported from `acts`, and the `acts` `store-*` cargo features (and their sqlx/redis/sled/async-nats dependencies) are removed; add `acts-store` with the matching feature (`cargo add acts-store --features sqlite`) and import the backend from `acts_store` (`use acts_store::SqliteStore;`). The `KvStore` trait, the in-memory `MemoryStore`, the `Store` façade and all row models stay in `acts`; pass the backend to `EngineBuilder::set_store(Arc<dyn KvStore>)` exactly as before
- test: the cross-backend store suite (previously `acts`'s `store::tests`) moved to `acts-store/tests` — one generated suite runs against memory, sqlite and sled in CI (nats/redis/postgres behind their features), and the engine-on-sqlite tests (`engine_set_store_sqlite`, `engine_sqlite_runs_on_current_thread_runtime`) moved there too
- test: the `acts-store` cross-backend suite no longer shares one backend instance across `#[tokio::test]`s — nats/redis/sqlx spawn their connection tasks on the runtime that is current at `open()`, so an instance created by the first test died with that test's runtime and every later test on those backends failed (`broken pipe` / `publish failed: channel closed`); each test now opens its own backend inside its own runtime, and the nats/redis/postgres suites (93 cases each, behind their features) pass against live services
- feat: snapshot-backed sealed data — external systems feed versioned values through `Engine::snapshot()` (`upsert`/`remove`/`read`) over any transport (gRPC/NATS/Kafka adapters); at each task's prepare the engine seals the matching entry from the local cache, so sealing never performs network I/O
- feat: snapshot policies — `SnapshotPolicy::PerProc` (default) seals once per task lineage and descendants inherit the pinned value (a retried/resumed task keeps its first value); `SnapshotPolicy::PerTask` re-reads the latest cache value at every new task
- feat: per-target scope keys — `SnapshotOptions::key_params` derive the snapshot scope from task params (parent-chain traversal), `on_missing` controls missing-param/absent-data behavior
- feat: registration — `EngineBuilder::add_snapshot`/`Engine::add_snapshot` pre-register targets; `upsert` on an unregistered name auto-registers it with default options
- feat: snapshot TTL — `SnapshotOptions::ttl_secs` expires entries not refreshed in time; expired values are dropped on read and by a periodic purge timer, so dead scopes cannot grow the cache unboundedly (pairs with `remove()` tombstones); `SnapshotEntry::ts_ms` renamed `timestamp`
- BREAKING: the callback-based `ConfigResolver` trait and `add_resolver`/`register_resolver` plumbing are removed — sealed data is snapshot-only; migrate by registering a target (`add_snapshot`) and feeding values through `Engine::snapshot().upsert()`
- feat(acts-channel): client helpers `ActsChannel::upsert_snapshot` / `remove_snapshot` to feed and delete snapshot data over gRPC
- feat(acts): the transport-agnostic name→engine action dispatch now lives in the engine as `acts::actions` — `apply(&Engine, name, Vars)` maps channel-message action names (`act:`/`model:`/`pack:`/`proc:`/`task:`/`msg:`/`evt:`/`snap:`) to engine operations and returns the JSON wire value, with `NotFound`/`Invalid`/`Internal` error kinds mapping to transport semantics; adds snapshot actions `snap:upsert` / `snap:remove` / `snap:get` (one scope, `null` when absent) / `snap:ls` (all scopes)
- feat(acts-plugin-grpc): `do_action` refactored onto the shared dispatch with unchanged wire/error semantics — snapshot feed and query are now available over gRPC; added an end-to-end test (client upsert/remove against a live plugin)
- feat(acts-plugin-nats, new): exposes the same message surface as the gRPC plugin over NATS core — actions are request/reply on `<subject>.cmd` through the shared dispatch, engine events are forwarded per configured `[[nats.channels]]` (filters mirror `MessageOptions`), ack/redelivery keeps the gRPC semantics (`msg:ack`); adds README and live tests that skip when no broker is reachable (`ACTS_NATS_URL`)
- feat(acts-plugin-web): `/api/snap/{upsert,remove,get,ls}` endpoints backed by the shared dispatch (missing scope answers `data: null`, same shape as the other transports)
- feat(acts-cli): new `snapshot` subcommand with `upsert` / `get` / `ls` / `remove`; adds README
- feat(acts-server): engine construction extracted into a library (`build_engine` + `ServerPlugins`) shared by the binary and its tests; the NATS plugin is registered when the config has a `[nats]` section; adds README and a NATS wiring test
- ci: the GitHub Actions test job runs a NATS service container and sets `ACTS_NATS_URL`, so the NATS live tests (plugin and acts-server wiring) run against a real broker — they auto-skip when no server is reachable
- fix: update `grpc`, `web` and `nats` config
- fix: add snapshot-backed sealed-data config in acts.toml - default set with `profile` and `secrets` in `acts-server`
- fix: adit error `RUSTSEC-2026-0258`

# 0.23.1
- fix: move `acts-plugin-common` to `acts` as `actions` and remove the origin common reference from all plugins 
- fix: `acts-server` dependencies version error for `acts-package-*` and `acts-plugin-*`

# 0.24.0
- feat: make sure `grpc` plugin is always included in `acts-server`
- feat(acts-server): the server config moves to `~/.acts/acts.toml` (`$ACTS_CONFIG_DIR` overrides) — on first start acts-server creates the directory and writes an embedded default config (the template is compiled into the binary, so a `cargo install acts-server` binary auto-creates it too); a local `./acts.toml` in the working directory deep-merges per key over the `~/.acts` defaults
- feat(acts-server): the `[db]` section is optional and defaults to the sled backend under the config dir; other stores are selected with `type` (`sqlite`/`postgres`/`redis`/`nats`) plus `database_url`, and the `ACTS_DATABASE_URL` env var overrides `database_url`; the server now compiles all `acts-store` backends
- feat(acts): `Config::overlay_file` deep-merges a second acts.toml over a loaded config (nested tables merge per field, scalars/arrays replace) so layered configs can override single options
- build: add a release workflow that builds `acts-server` and `acts-cli` for linux-x86_64 / macos-x86_64 / macos-aarch64 / windows-x86_64 and uploads them to the GitHub release on every `v*` tag
- fix: a vars mutation can no longer be lost to a stale dirty-flag clear — a task scope's vars row is written, then the scope's dirty flag is cleared only when no mutation raced the write (a per-task generation counter is bumped by every vars mutation and compared across the durable write); previously `persist_task_rows` cleared the flag unconditionally after the awaited store write, so a concurrent `set_sign`/`set_data` landing mid-write could be cleared without its data ever being persisted — e.g. the `NEXT_COMPLETE` marker was durably dropped while the outbox record closed `Done`, so crash recovery lost its idempotent-replay guard and re-ran an already-completed `next`
- fix: the postgres store compares keys byte-ordered — the `key` column is created `TEXT COLLATE "C"` (previously the database locale collation, e.g. en_US.UTF-8, weighed the `KEY_SEP`/`KEY_SEP_SUCC` separator bytes equal and broke the half-open `[lower, upper)` index scans at exact bounds: a `Gt` included the bound value and an inclusive `Between` dropped its upper row); pre-existing tables keep their old collation — recreate or `ALTER TABLE ... ALTER COLUMN key TYPE text COLLATE "C"` once

# 0.24.1
- fix: `schedule` triggers arm to their actual next cron fire instead of firing on the first engine tick — a freshly deployed or cron-changed schedule row now stores `next_run = cron.next()` (not `now`), so it fires at its first cron boundary, and re-deploying an unchanged schedule deterministically keeps `last_run`/`next_run` (previously the armed-now row fired immediately and rolled the run state between the deploy and the re-deploy, racing the reconcile)
- ci: the release workflow builds `macos-x86_64` by cross-compiling `x86_64-apple-darwin` on `macos-latest` instead of the retired `macos-13` runners (jobs queued forever waiting for a runner that no longer exists); every target now declares its rust `target` triple and builds with `--target`


# 0.25.0
- fix: shell, HTTP and NATS acts are bounded in time and by output — a workflow node could run forever and hold one of the scheduler's fixed number of lanes with it: `[shell] timeout-ms` (default 300000, `1..=3600000`) is a deadline for one shell act, `[shell] max-output-bytes` (default 1048576 per stream, `1..=67108864`) caps what a script may capture, `[http] timeout-ms` (default 30000, `1..=3600000`) is the whole-request deadline HTTP requests no longer get to skip, and `[nats] timeout-ms` (default 30000, `1..=3600000`) bounds connecting, publishing, subscribing and receiving; every bound is always in force (no value disables it) and a value outside its range is a startup error rather than a silent clamp
- fix: a shell act that hits its deadline or its capture limit kills the child and waits for it, and no longer waits for the other to finish — previously the two streams were read and the child waited on with `join!`, so a script that kept writing past the capture limit was never waited up on and a script that closed its output but kept running was waited on forever, and the act held its lane in both cases; a failing stream now terminates the child first (the wait reaps it, so no zombie is left), the child's own wait is bounded by the same deadline, and cancellation kills it too
- feat: `Context::cancellation_token()` gives a running act a stop signal — the engine fires it when it shuts down, and when an action (`abort`, `cancel`, `skip`, `remove`, `next`, `error`) overrides the task while the act runs, so the work an act started outside the engine (a child process, a request, a subscription) stops with the task instead of outliving it and holding its lane until the act's own deadline; `Task` now owns a token that is a child of the runtime's shutdown token, `Task::set_state` fires it on a running task's terminal transition, and a cancelled act reports no failure of its own: the action owns the task's state and a shutdown leaves the task running for the next start to resume
- fix(scheduler): an act's own error can no longer overwrite the state an action decided — an act that is aborted (or cancelled/skipped/removed/next-ed) while it runs now stops and reports an error on its way out, and `run_exec_job` applied it unconditionally, turning a workflow's own `abort` into an `Error`; the error of an act whose task is already terminal is logged and dropped instead
- BREAKING: `acts-package-http` no longer leaves a request unbounded — `[http].timeout-ms` defaults to 30000 and `connect-timeout-ms` to 10000, neither can be set to 0 to disable it, and a value above the platform ceiling (3600000 / 300000) fails startup; the act's own `timeout-ms` param may now only *tighten* the configured value (a value above it, or zero, fails the act), instead of replacing it with anything up to unbounded; a request that stalls mid-body after its headers now ends at the deadline (the total timeout does cover the body) and the body-read error carries its source chain, so a timeout is no longer reported as "error decoding response body"
- feat: `acts-package-nats` bounds every wait it performs — connecting, publishing, subscribing and receiving a message are raced against `[nats].timeout-ms` (default 30000) and the act's cancellation, so a broker that never answers or a subject nothing publishes to fails the act instead of holding its lane; a subscribe that times out or is cancelled drops its subscription with the act
- fix: `acts-package-shell` output capture is capped by default and no longer has a per-act `max-output-bytes` param — the bound is the deployment's (`[shell].max-output-bytes`), so a workflow cannot widen or disable it; `[shell].timeout-ms` and `[shell].max-output-bytes` join the allow/deny policy in the same section, and `DEFAULT_*`/`MAX_*` constants are exported from the package crates (`acts_package_shell::{DEFAULT_TIMEOUT_MS, MAX_TIMEOUT_MS, DEFAULT_MAX_OUTPUT_BYTES, MAX_OUTPUT_BYTES}`, `acts_package_http::{DEFAULT_TIMEOUT_MS, MAX_TIMEOUT_MS, MAX_CONNECT_TIMEOUT_MS}`, `acts_package_nats::{DEFAULT_TIMEOUT_MS, MAX_TIMEOUT_MS}`)
- test: `acts-package-shell`'s lane release is pinned against a writer that never stops — `tests/bounds.rs` gains three cases whose script cannot end by itself (`while ($true) { 'xxx…' }` on Windows, `yes` elsewhere; the existing `flood` helper is a bounded loop on Windows, so it exited on its own and an act that returned proved nothing about what the act did with it), with the engine pinned to one lane for the first two: an endless flood on `stdout` must fail at `[shell].max-output-bytes = 4096` and leave that lane able to run an ordinary act, an endless flood on `stderr` alone must fail the same way (which `flood(true)` does not cover on Windows, where it writes both streams), and four floods on four lanes must all fail at the limit with the engine still serving work afterwards; the harness gains `run_shells` for the last one, because a channel handler sees every event and a per-run signal cannot tell whose end arrived, so the wait counts terminal events and the outcomes are read back by pid, and every case runs under a `tokio::time::timeout` guard so the pre-fix wait — the pipes stay open, the flooded child blocks on a full pipe, and the act holds its lane until the deadline — fails the suite instead of hanging it (run against exactly that wait, the three fail at 30.06s, 30.03s and 60.03s, and the suite is green again once it is removed)
- security: `acts-package-http` enforces an egress policy so a workflow-controlled URL cannot reach the host's own network — an optional `[http]` config section sets `allowed-hosts`, `allow-private-addresses`, `connect-timeout-ms`, `timeout-ms` and `max-response-bytes`; a request is rejected when its target is loopback/private/link-local/carrier-grade-NAT/unique-local or a cloud metadata endpoint (169.254.169.254, 100.100.100.200, fd00:ec2::254), the check runs on IP literals and again on the resolved addresses actually dialed, and redirects are re-validated; response bodies are streamed with a 64 MiB default cap instead of read whole, so an oversized or never-ending body fails the act instead of exhausting memory — this is a breaking default: endpoint targets reachable only on loopback/private networks now need `allow-private-addresses = true`
- perf(scheduler): replace the single serialized event-loop executor with fixed pid-hashed task lanes — independent processes now overlap up to `scheduler_workers` jobs while work for one pid stays FIFO; each lane owns its own bounded job queue and runs it serially, so scheduler jobs and same-pid workflow event handlers share a lane gate to preserve completion/action ordering
- fix(scheduler): the lanes are actually bounded — the previous fixed lanes used unbounded channels, so a slow task absorbed its whole fan-out in memory and the `scheduler_queue_cap` → durable-outbox path was reachable only by producers racing the dispatcher, never by a slow consumer, while `scheduler_queue_depth`/`high_watermark` reported the dispatcher's instantaneous backlog instead of the real one; each lane now has `scheduler_queue_cap / scheduler_workers` slots (at least one), a full lane answers `QueueFull` to its producer so the durable outbox is the real overflow, the depth/high-watermark metrics read the lane channels themselves (so they cannot drift past the cap; `scheduler_lane_capacity()` reports the per-lane bound), and a lane worker whose channel is closed is reported to producers instead of buffering work nothing will run
- perf(store): reduce query materialization and round trips — extract collection ids directly from data keys instead of parsing every row, paginate indexed `order_by` queries from the ordered index while reading only the page documents, batch page reads through `KvStore::mget` (Redis `MGET`, SQL `IN`, Sled blocking batch), and evaluate AND filters by candidate cardinality with empty-branch short-circuiting
- fix(store): the ordered-index `order_by` path no longer trusts `KvStore::scan_prefix` entry order — it rebuilt value groups from consecutive scan entries, which only holds for backends with ordered iteration, so on Redis (`SCAN` returns keys in arbitrary order) `order_by` on an indexed field (`Message`/`Task`/`Proc`/`Package`/`Model` timestamps, `Event.next_run`, `Delivery.update_time`) returned rows in scan order and every indexed range/`between` page came back unsorted; entries are now grouped in a `BTreeMap` by encoded value with an id-ascending tie-break inside each group (null/missing first ascending, last descending), matching `cmp_order_docs`; `KvStore::scan_prefix` documents that entry order is unspecified, and a regression test scans through a `KvStore` whose `scan_prefix` reverses its entries
- fix: add index fields `["kind", "next_run", "mid"]` for `event` collection and add index fields `update_time` for `deliveries`
- perf: cache registered package instances per engine registration instead of recreating them for every Func act or custom event trigger; package constructors now run once per successful first use and replaced registrations get a fresh cache slot
- perf: `acts-package-state` uses a long-lived Redis multiplexed async connection and async `GET`/`SET` instead of opening and blocking a synchronous connection on every execution
- perf: `acts-package-http` uses a package-level async `reqwest::Client`, reusing connections and TLS sessions instead of creating a blocking client for every HTTP act
- fix: `acts-package-http` is registered as a `Func` act so the engine executes its package handler instead of leaving the task interrupted
- perf: `acts-package-shell` runs child processes with `tokio::process` and captures stdout/stderr asynchronously without blocking a scheduler worker
- fix: `acts-package-nats` no longer creates a private Tokio runtime or calls `block_in_place`; it connects lazily on first async execution and reuses the NATS client, avoiding the current-thread runtime panic and blocking a scheduler worker
- fix: break the `Process → TaskTree → Task → Process` reference cycle — a `Task` now holds its process `Weak`ly, so a finished process evicted from the cache is actually deallocated together with its whole task tree; previously the cycle kept every finished process and all of its tasks (with their scope data) alive forever as unreachable cyclic garbage — `evict` freed the resident-set slot but never the memory, so the `cache_cap` park/refill design still leaked unboundedly
- BREAKING: `Task::proc()` returns `Option<Arc<Process>>` instead of `&Arc<Process>` — `None` only for a task clone that outlived its evicted process (engine-driven paths always see a live process); a late root-task write from the store writer whose process is already gone falls back to the task's own state to stamp the proc row complete, so the sweeper still removes it
- test: `cache_evict_breaks_proc_task_cycle` proves both sides of the contract — the task tree stays queryable while the caller holds the process, and the process is deallocated once the last holder drops
- fix: queue items now carry a process execution lease, so a queued task can still execute when a concurrent terminal event evicts its process before the scheduler reaches the item
- fix: scheduler task execution is panic-isolated and takes the ordinary task-error path, while queue producers fail after the event loop exits instead of silently accumulating unbounded work
- fix: JS value conversion propagates allocation/conversion errors instead of panicking; oversized BigInt results are rejected rather than silently wrapped
- fix: remove unsafe impl Send/Sync
- fix: store `Between`/`In` fallback filters compare integers exactly — `cmp_json_val` now uses the same exact numeric ordering as `order_by` instead of lossy `f64` conversion, so adjacent integers above `2^53` (including the full `u64` range) no longer compare equal in non-indexed scans
- fix: store numeric range fallback filters handle mixed numeric types exactly — `LT`/`LE`/`GT`/`GE` now reuse the exact `order_by` numeric comparator instead of coercing the right side through the left side's integer type or `f64`, so cases such as `3 < 3.5`, `5 < u64::MAX`, and `i64::MAX < u64::MAX` no longer produce false negatives
- fix: atomically claim per-pid cache-miss single-flight, so concurrent callers cannot pass the empty check before another leader inserts; every waiter now receives the same loaded `Arc<Process>` instead of racing duplicate process instances
- fix: admit external process ids through an atomic in-process pid claim, so concurrent starts that all miss the durable row fail as duplicates instead of creating two running or parked instances
- fix: invalid `ChannelOptions` globs no longer panic the public `Channel::channel` / `engine.channel_with_options` path — invalid `type`, `state`, or `uses` patterns fall back to `*` with a warning (so remote channel-query input cannot turn an unclosed bracket into a handler panic), while invalid custom `options` globs remain skipped
- fix: `Config::create` and `EngineBuilder::set_config_source` now return config errors instead of panicking on missing, unreadable, or malformed files; the builder's implicit default config falls back to defaults with a warning
- fix: system environment values that cannot deserialize in `Context::get_env` are treated as absent with a warning instead of panicking the scheduler
- fix: web and gRPC plugins no longer use `unwrap` for bind/serve, socket parsing, remote-peer access, or channel-message serialization; malformed input and runtime transport failures are logged or returned as `Status` errors
- BREAKING: `Engine` now represents a successfully started engine only — configuration lives solely on `EngineBuilder`, `EngineBuilder::start().await` returns `Engine`, and `Engine::new()`/`Engine::start()` are removed; update `Engine::new().start()` to `Engine::builder().start()`
- fix: Index `Eq/Range` scans without value pushdown: all backends scan the entire field region, and SQLite/Postgres also return rows for the entire field region
- perf: cache package definitions and compiled JSON Schema validators in `Runtime`, keyed by act `uses`; repeated `Irq`/`Msg`/`Func` acts no longer perform a package store lookup, reparse the schema text, or recompile the validator on every execution
- perf: cache `ActSchema` validators by serialized schema content, reusing compiled validators for repeated workflow input/output validation
- perf: share process workflow models with `Arc`, avoid deep-cloning the whole workflow while creating or emitting task messages, and reuse one model clone during process startup/build/restoration
- feat: validate `Func` package params against the package JSON Schema before creating and executing the package instance
- fix: invalidate a runtime's cached package definition when the package is published, removed, or re-registered through the engine extender
- perf: pool QuickJS contexts in the expression environment and reuse initialized built-in modules instead of recreating a runtime, context, and all modules for every `${{...}}` expression
- fix: refresh task vars, user vars and sealed data before every pooled expression, and restore isolated global state so one expression cannot leak globals into the next
- perf: linearize `Task::vars` parent-chain merging and reduce redundant `Vars` cloning in JSON conversion and expression filling
- perf: use borrowed scheduler-context access for expression evaluation instead of cloning the scoped context for each nested call
- perf(acts-store): move sled reads, writes, batches and scans onto tokio blocking threads so disk I/O no longer occupies async workers; batches keep one atomic apply-and-flush while single puts/deletes preserve their existing durability window
- BREAKING: `Context::scope` now takes `&Context`; update callers from `Context::scope(context, ...)` to `Context::scope(&context, ...)`
- perf(acts-store): pool SQLite file connections in WAL mode so reads can run concurrently and writes no longer queue behind large scans; batches keep their atomic `BEGIN IMMEDIATE` semantics while in-memory stores retain one shared pooled connection
- fix(plugins): SSE and gRPC transports deregister their channel handler when the client disconnects — the SSE response body and the gRPC `on_message` response stream each carry a drop guard that calls `Channel::close()`, so finished connections no longer leak handlers into the emitter map (previously every dead client kept matching all future messages: unbounded map growth, per-message O(all historical channels) glob matching, a doomed `tokio::spawn` send per dead SSE channel, and three store writes plus one `messages().exists` read per message per dead ack channel); the SSE stream also ends cleanly instead of spinning on its closed queue once the handler is deregistered; regression tests on both transports use ack-delivery message rows as the leak detector (a live channel stores one row per message, a dropped one stores none)
- feat(scheduler): bound the in-memory scheduler queue with `scheduler_queue_cap` (default `4096`, configurable through `Config`, `EngineBuilder`, and the acts-server template) and expose queue capacity/depth/high-watermark plus store-writer backlog depth/high-watermark
- fix(scheduler): overflow durable `next` work to the outbox and replay it after the queue drains; overflow fresh task starts to `Exec` outbox records for retry; when the queue remains full at startup, overflow records are retained instead of losing work
- BREAKING: `ActError` gains a `QueueFull` variant, and outbox records gain `exec`/`dispatched`/`overflow` states for scheduler overload recovery
- perf(cache): admission and restore passes no longer serialize on one global async mutex held across store I/O — `admit` now makes its capacity decision synchronously and parks outside the lock, while `start_parked`/`resume`/`resume_from_queue` reserve the free slots atomically and then load rows and call `Process::start` with no lock held, so a slow store no longer blocks every new start behind a restore (a second restore pass sees the slots as taken and skips instead of blocking or reloading the same rows, preserving the "two concurrent passes never start one row twice" invariant); regression tests park a store read and prove `admit` completes while it is held and that a concurrent pass neither blocks nor double-loads
- fix: `Back`/`Cancel` register the replacement task before the rewind marks the history path terminal — previously `back_task`/`undo_task` left every child of the process `Completed`/`Backed` with the redo task not yet created, so a concurrently re-dispatched `next` pass (recovery replays the durable records) could observe all children terminal, complete the root task, and stamp the whole process finished while the redo child was still in flight — the redo task then became an orphan under a terminal (and soon swept) process, and the failure surfaced as an intermittent missing process row in `sch_action_recover_reapplies_back`/`_cancel`; `redo_task` is synchronous, so the new child is registered before the first `emit_task` await
- fix: a cache miss whose `flush` overlaps another caller's load now reuses that load's instance — the single-flight leadership claim happens after `flush`, so a caller that missed before the leader cached the process could claim leadership once the leader's in-flight entry was removed, run a second load, and return a second `Arc<Process>` for one pid (two instances driving the same durable process); the leadership critical section now re-checks the resident set under the same `loading` lock, which is safe because the leader inserts resident before removing its in-flight entry; `cache_proc_miss_overlapping_load_reuses_instance` reproduces the exact interleaving deterministically with a store that gates reads and writes separately
- fix: dedupe the boot-resume overflow queue — repeated overflow scans no longer enqueue the same in-flight pid again, so the queue stays bounded by overflow rows instead of growing with the number of scans, and a queued pid costs one store lookup per free slot rather than one per scan
- fix: a per-process event worker that exits on its terminal event no longer drops events accepted during the terminal handler — previously the worker broke out of its loop and only then removed itself from the routing map, so an event routed in that window (e.g. a retry-timer delivery or completion for the same pid) was sent into a receiver nobody read; the exit path now removes the worker under the routing write lock and re-homes every dequeued-but-unconsumed event to a fresh worker, preserving their order ahead of later events
- fix(scheduler): a step with `uses` no longer completes before its act child propagated — `check_uses_action` counted a child as done from its terminal state alone, but that state is written by whichever job applied the child's action (the `acts.core.action` package sets `Submitted` inside the child's own `exec`, a client ack sets `Completed`) while the child's `next` is dispatched as a separate job; the step's `next` could therefore run first, complete the step and the whole workflow with the child's outputs missing from the step's data (`pack_action_sumit_on_step_with_inputs` observed the action's `a: 5` absent from the step under scheduler load); the child now counts only once its own `next` set `NEXT_COMPLETE`, which is exactly the propagation that merges the outputs, and the re-entered step `next` proceeds when it observes the marker; `sch_step_uses_waits_for_child_next_before_complete` drives the interleaving deterministically
- fix: snapshot `upsert` enforces revision monotonicity per scope — the write lock now compares against the cached entry and drops any delivery whose `rev` is not newer, so a delayed network retry, an out-of-order bus delivery, or a race between two feeds can no longer roll a scope back to an older value (previously the entry was overwritten unconditionally and a late `rev=1` clobbered a stored `rev=2`); an equal revision is an explicit idempotent replay (cached value kept, ttl basis refreshed), and `SnapshotStore::upsert` documents the policy with unit tests for the stale/duplicate paths plus a barrier-interleaved concurrent-writer test
- fix: the snapshot ttl read path no longer drops a concurrently refreshed value — `get` used to clone the expired entry under a read lock and then `remove` the scope unconditionally under the write lock, so a feed `upsert` landing in that window (a task prepare reading the expired value while the feed refreshes the same scope) was silently deleted and the scope read as absent again; the eviction now re-checks under the write lock that the cached entry is still the exact one observed (`rev` + `timestamp`) and, when a refresh replaced it, returns the refreshed value to the reader instead of dropping it; expiry is defined once (`is_expired`) and shared with the purge sweep; `expired_read_does_not_drop_a_concurrent_refresh` reproduces the interleaving deterministically (`evict_expired` with an entry observed before the refresh) and `expired_read_and_refresh_race_keeps_the_refresh` runs a barrier-interleaved reader/writer over 64 scopes
- fix(acts-channel): the gRPC client no longer panics on protocol anomalies and no longer loses subscription failures — `ActsChannel::send`/`start` decoded the response body with `unwrap`, so a malformed server response panicked the caller's task; the handshake `unwrap` in the subscription path turned a failed `OnMessage` RPC into a panic; and the background feed task silently dropped stream errors and panicked on a corrupt message payload, ending the subscription with no notification at all. Responses now decode through `decode_payload`, mapping a malformed body to `Status::internal` (absent body still yields `ActionResult::data: None`); `subscribe(client_id, on_message, on_error, options)` returns `Result<Subscription, Status>` — the handshake failure reaches the caller, faults that do not end the feed (a payload that fails to decode, skipped and left unacked so the server can redeliver; a failed auto-ack, message not handed to the callback) go to the new `on_error` callback as `SubscriptionError`, and the end of the feed is reported by `Subscription::wait()` (`Ok(())` clean close, `Err(status)` on a stream/connection failure); the CLI `message sub` prints faults and the feed's end to stderr; tests in `crates/acts-channel/src/tests/subscribe.rs` drive a mock service through a live gRPC round trip (malformed unary body, decode fault followed by a delivered message, clean close, failed handshake, failed auto-ack) and fail on the pre-fix code
- fix(acts-server): a malformed `snapshot` config is now a startup error instead of a panic — `engine_builder` read the section with `Config::get::<Vec<SnapshotConfig>>("snapshot").expect(...)`, so a scalar (`snapshot = "bad"`) or a single `[snapshot]` table where the `[[snapshot]]` array of tables is expected aborted the process with a panic backtrace before the engine started; the builder now returns `acts::Result<EngineBuilder>` and propagates the `ActError::Config` (the value is named in the message, e.g. `failed to get 'snapshot' config: invalid type: string "bad", expected a sequence`), so a deployment typo fails startup diagnosably (`acts-server` exits 1); `main` and the NATS wiring test propagate the error, and a regression test covers the scalar-string, scalar-int and single-table shapes
- fix(acts-server): Ctrl-C/SIGTERM now shut the server down gracefully — `main` awaited a private `engine.signal(())` that nothing ever fired, so the process only stopped by force-kill and `Engine::close` never ran (the store writer was not flushed and timers/background tasks were killed mid-flight); `main` now selects on Ctrl-C plus SIGTERM (unix) and awaits `engine.close()` before returning
- feat(acts): `Engine::shutdown_token()` exposes the runtime shutdown token (re-exported as `acts::CancellationToken`) so plugins and embedders can stop their own long-running tasks when the engine closes
- fix(plugins): the gRPC (`serve_with_shutdown`), web (`with_graceful_shutdown`) and NATS (action subscription) transports stop on the engine shutdown token instead of running until the process is killed, so their sockets and the NATS connection close during a graceful shutdown
- fix(acts-channel): `Vars::value_str` / `value_number` treat a wrong-typed field as a miss instead of panicking — both mapped the key through `as_str().unwrap()` / `as_f64().unwrap()`, so a key holding a number/bool/object (e.g. vars received from a remote peer or plugin) aborted the caller; they now use `and_then` and return `None`, matching the generic `get`'s `Option` contract, with a regression test covering type mismatch and absence
- fix(acts-server): an unusable `log.dir` is now a startup error instead of a panic — `init_log` created the log directory with `create_dir_all(...).unwrap_or_else(|err| panic!(...))`, so a read-only directory, a path that is already a regular file, or an unavailable mount point (external config/permission state, not an internal invariant) aborted the process with a backtrace; `init_log` now returns `Result` and `main` propagates the `anyhow` error with the path and OS cause (`failed to create log dir <dir>`), so a supervisor sees the reason and `acts-server` exits 1; a regression test starts the real binary against a `log.dir` under a regular file and asserts non-zero exit, the diagnostic, and no `panicked` in stderr
- fix: a document's data row and its index rows can no longer be torn apart by concurrent writes — `update`/`delete` (and every multi-row batch built on them: `deploy`/`rm_model`, `remove_proc_rows`, `upsert_proc_with_task`, the model→trigger reconciliation, `rebuild_index`) read the stored document to compute which index rows to drop and then committed the data row plus the new index rows as one batch; two writers of the same id could both compute from the same stored version, so whichever batch landed second left index rows of a value the data row no longer held (a query returned a phantom id whose document does not exist) or dropped index rows of the value it did hold (a query missed a row that is stored). A document is now locked from its read until its batch is applied (`lock_docs`: process-wide registry keyed by the full document key, self-cleaning, keys acquired in ascending order so two mutations that touch several documents in a different order cannot deadlock), so the index rows of an id always describe the document that is stored under it; the multi-row paths lock every row they rewrite before reading it, and lock a model before the trigger rows it owns (no path takes a trigger lock before a model lock)
- fix: `DbCollection::create` now drops the stale index rows of the document it replaces, making `create` and `update` the same index-consistent write — `create` used to write only the data row and the new index rows, so landing on an id that already held a document (the engine's find-then-create upserts — `publish`, `upsert_proc`, the task/vars rows — and two racing callers of either) left the previous version's index rows behind, answering queries for a value the stored row no longer held; an insert into an id with no stored document is unaffected
- fix: recovery and restore no longer drop rows past a page limit — `Query::new()` defaults to `limit: 100000` and `DbCollection::query` answers at most that many rows (the size of the full match set is reported only as `count`), while the crash-replay read (`load_pending_ops`), the overflow replay (`load_overflow_ops`), the outbox state transitions (`enqueue_op`'s pending-dedup, `mark_op_dispatched`, `complete_ops`, `mark_op_overflow`), a restored process's task and vars rows, the model→trigger reconciliation and the boot-resume overflow queue all treated the returned rows as the complete match set — a pid with more than 100000 outbox records or task rows therefore lost every row past the limit: pending `next`/`action` operations were never replayed, task rows were never restored, and in-flight pids beyond the resident cap were never queued (that queue is their only path back). `DbCollection::query_all` reads the whole match set (ids from the same index/filter path, `query.limit` only sizes one read batch, `query.offset` is not applied), and every recovery/reconciliation path reads through it
- fix: cleanup no longer leaves orphans past a page limit — `remove_ops`, `clear_error_deliveries`, `rm_model`'s trigger rows, `remove_proc_rows`' five per-pid collections and `has_unsettled_deliveries` (the check that gates the sweeper) got their ids from the same limited `query`, so a process with more than 100000 outbox or delivery rows kept the remainder: task/vars/outbox/message/delivery rows (with their index rows) survived the process — resurrecting it as a broken process on the next restore and keeping its deliveries retried — and an unsettled delivery that fell past the limit let the sweeper delete a process whose delivery was still open. They now enumerate the whole match set through `DbCollection::matching_ids`/`delete_all` (each row still commits on its own, index rows and data row as one batch), and `Query::limit` is documented as a page size rather than a completeness bound
- test(store): `recovery_and_removal_are_exhaustive_past_one_query_page` seeds 100001 outbox rows for one pid (one more than the default page), asserts the page read really stops at 100000 while `load_pending_ops` returns all 100001, then removes the process and asserts no outbox data or index row survives; `query_all_reads_past_the_page_limit_in_order` and `delete_all_removes_every_match_and_its_index_rows` cover the primitives directly
- fix: a store backend failure can no longer masquerade as a missing row, an empty result, or a successful write — `DbCollection::find` reported both "no such document" and "the store is unavailable" with the same `ActError::Store`, so callers used `if let Ok`/`Err(_) => Ok(None)` and swallowed every backend error: process load returned `Ok(None)` (indistinguishable from an absent process), the delivery updates (`mark_delivered`, `set_delivery`, `set_deliveries_with`), the no-response retry scan and the error-delivery resend returned success with nothing written (stuck deliveries never re-armed and never surfaced), `resend_error_delivery`/`clear_error_delivery` reported "not found", the task/vars/proc upserts fell into the `create` branch on any error, and the boot-resume queue treated every error as "removed while queued". `DbCollection` gains `find_opt`, which normalizes only the explicit absent-row case to `Ok(None)` (a backend error stays `Err`); `find` keeps its error contract by delegating to it, and the affected paths now propagate and log the error — a down backend is visible and the retry timer keeps its next tick instead of the failure hiding as settled business state
- BREAKING: `DbCollection` gains a required method `find_opt(&self, id: &str) -> Result<Option<Self::Item>>` — an out-of-tree implementation must add it (only the explicit not-found case may return `Ok(None)`; every backend failure must stay an error)
- test(store): `cache_store_error_is_not_an_empty_success` injects a `KvStore` whose every call fails and asserts `load_proc`, `set_delivery`/`mark_delivered` and `with_no_response_deliveries` all return the store error rather than a silent `Ok`; `find_opt_normalizes_only_missing_rows` pins the `find_opt`/`find` split
- fix: a step's timeout branch fires at most once per timed-out task — `Process::do_tick` found every in-flight step that declares `timeouts` and `Step::on_timeout` dispatched its branch unconditionally, so a step that stayed `Running`/`Interrupt` while a branch's guard held (e.g. `$cost_in('1d')`, or any branch without an `if`) created a fresh branch task on every tick: the branch's act was re-dispatched each time (IRQ/`msg`/`action` side effects repeated — an external storm) and the task/outbox rows grew with the tick count. A branch now claims a one-shot slot on the timed-out task when its guards hold, keyed by the branch's node id and persisted with the task's vars row (`__timeouts`) before the branch's effect is dispatched, so later ticks and a restored process both skip it; distinct branches keep distinct slots (`timeouts: [1d, 2h]` still fires each once). The tick dispatches the first branch that has not fired (a branch whose window has not opened is skipped, not fired) and never duplicates a branch that is still queued or running, and it stops dispatching once every declared branch has fired instead of creating a task per tick. The one-shot state is this per-branch marker; `Sign::TIMEOUT` remains an unused general flag bit
- test(scheduler): `sch_step_timeout_fires_each_branch_once_across_ticks_and_restart` ticks a running two-branch step 5 times (and 3 more after the branches settled) and asserts one child per branch, then restarts an engine on the same store and asserts the restored process creates none; `sch_step_timeout_does_not_repeat_a_fired_branch` pins the effect side with an always-true guard and `uses: msg`, asserting exactly one message across 5 ticks. Both fail pre-fix with 5 children for 5 ticks
- fix: the terminal write no longer erases a failed delivery — a delivery whose retries were exhausted is marked `Error` for manual resend/clear, but the task's terminal write (`WriteOp::Task`) and the deferred action/task close rewrote every delivery row of the task to `Completed` unconditionally, so the error was silently dropped and `has_unsettled_deliveries` could approve the process for the sweeper while a manual redeliver had no delivery object left; the close is now an exhaustive, conditional, per-document-locked update (`Store::close_deliveries(pid, tid)`, replacing `set_deliveries_with`/`upsert_message_status`) that settles only `Created`/`Delivered`/`Acked` and keeps `Error` open, and because each row is re-read under its document lock a concurrent retry-timer `Error` write can no longer be overwritten by a close that read the row before it; `close_deliveries_preserves_error_rows` (store) and `delivery_close_preserves_error_rows` (writer) pin the preserved row, the untouched row of another task, and the process staying unmarkable/unswepth until the error row is cleared manually
- fix: the delivery retry pass and the ack/delivery writes can no longer overwrite a newer delivery state with a stale read — `with_no_response_deliveries` scanned a candidate page of overdue rows with one unlocked read and then committed `Delivered` (re-armed) or `Error` from that snapshot, while the engine's close, the channel handler's `mark_delivered` and the client's `set_delivery` read-modify-wrote the same rows unlocked: a retry tick that read a row before a close re-armed it after the task finished (a duplicate send, up to a resurrected `Error`), and an ack that read `Delivered` just before a close committed `Acked` on top of it, leaving a row nothing would ever settle (the process stayed unsettled forever). All delivery transitions now go through one serialized conditional update (`Store::rewrite_deliveries`/`update_delivery`): the row is locked before it is read, `update_time` is stamped at write time, and the whole row set commits as one batch, so every decision is taken on the stored row; the retry pass additionally skips a row whose `update_time` moved past the scan's bound (a manual resend or a successful dispatch landing in the window), so a message just handed over is not re-sent immediately; `retry_scan_does_not_overwrite_a_concurrent_close`, `retry_scan_skips_a_row_rewritten_after_its_candidate_scan` and `ack_waits_for_a_close_in_flight_and_keeps_it_closed` pin each interleaving behind a gateable `KvStore`, and `retry_scan_rearms_open_rows_and_errors_the_exhausted` pins the transition table (settled rows untouched, exhausted rows kept as `Error`, one page per pass)
- test(event): `event_terminal_exit_rehomes_queued_delivery` pins the retry-timer case of the worker-exit re-home: a delivery routed to a process while its terminal handler runs reaches its `chan_id` handler exactly once (the message-event variant was covered, the delivery route was not); fails against the pre-re-home exit path, which dropped the queued delivery with the receiver and left the row to burn another retry attempt on the next timer window
- fix: an event routed to a process whose worker entry is stale is no longer dropped — `Emitter::route`'s slow path (under the routing write lock) sent into the entry with `let _ = tx.send(event)`, so a sender that outlived its receiver (a worker task that ended without the exit path that removes and replaces the entry, e.g. aborted with the runtime) swallowed the event with no trace: a completion, message or retry-timer delivery was lost, and a delivery row waited out another full retry window on a budget that may already be spent; the send's failure is now recovered like the read-path miss and falls through to install a fresh worker, which is handed the event. `event_route_replaces_an_abandoned_worker` pins it: after the worker entry is abandoned (test hook `abandon_worker` installs a sender whose receiver is dropped, the state a killed worker leaves), the next event still reaches its handler exactly once; fails against the pre-fix `let _ =` send
- fix(plugins): a malformed action payload is rejected instead of silently running the action's global branch — the gRPC server parsed `Message.data` with `serde_json::from_slice::<Vars>(data).unwrap_or_default()` and the NATS plugin converted `data` through `From<JsonValue> for Vars`, which returns an empty `Vars` for anything that is not an object; a malformed JSON body or a non-object `data` (`[]`, `"msg:clear"`, `null`, `7`) therefore became empty options and the caller's input error executed the action's global branch — `msg:clear` deleted every error delivery and `msg:redo` re-armed them all, both answering success. gRPC now maps a parse failure to `INVALID_ARGUMENT` (`invalid message data: ...`) and NATS rejects a non-object `data` with an error reply ("invalid payload: data must be a JSON object") on the request's reply subject without applying the action; absent `data` still means empty options, which is the genuine no-payload case. `test_do_action_rejects_malformed_data` (rejects `[]`/`{`/`null`/`"msg:clear"`, accepts `None`/`{}`, asserts nothing was applied) and the NATS `action_options_*` unit tests plus the live `malformed_action_data_rejected` over-the-wire test fail against the pre-fix `unwrap_or_default`/`Vars::from` behavior
- fix: the snapshot ttl is evaluated without overflow and unrepresentable values are rejected — `is_expired` computed `ttl as i64 * 1000`, so a huge `ttl_secs` (config or API) truncated to a negative window or overflowed (a debug-build panic; a release-build window whose sign/scale depended on the value), and the strict `>` left a zero ttl readable for the rest of the current millisecond; the ttl is now precomputed once as a saturating `u64` millisecond window and compared against the elapsed time clamped at zero, with an inclusive boundary (an entry expires once the elapsed time reaches the ttl, so `ttl_secs = 0` means an entry is never valid) — the arithmetic stays total for every `u64` even though direct struct construction bypasses validation, so no build profile can panic or produce a different window; `SnapshotOptions::validate` (plus the new `acts::MAX_TTL_SECS`, `i64::MAX / 1000`, the largest ttl representable as an `i64` epoch-millisecond deadline) rejects anything larger, `Runtime::register_snapshot`/`SnapshotManager::register`/`Engine::add_snapshot` return `ActError::Config` instead of storing it, and `engine_builder` rejects an out-of-range `[[snapshot]] ttl` at startup; the config `ttl` parser multiplies with `checked_mul`, so an overflowing duration is a parse error instead of a panic
- BREAKING: `Engine::add_snapshot` and `SnapshotManager::register` now return `acts::Result<()>` and validate their options, `SnapshotManager::upsert`/`remove` return `acts::Result<()>`, and `SnapshotManager::remove` fails with `ActError::Runtime` when the target is not registered (previously a silent no-op — a tombstone for a name the engine never registered is a wiring error); `upsert` still auto-registers with default options and now propagates that registration instead of panicking on it; `acts::MAX_TTL_SECS` is exported, and the `snap:upsert`/`snap:remove` channel actions report these failures to the caller instead of always answering `true`
- test(acts-store): the NATS backend suite skips an unreachable server instead of reporting 93 failures — `tests/nats.rs` connected to a hardcoded `nats://127.0.0.1:4222` (`ACTS_NATS_URL` was ignored) and the shared `store()` of `gen_store_tests!` unwrapped the connect, so with no NATS server running every case panicked in setup with `os error 10061` and no run distinguished an unavailable broker from a failing assertion; the suite now reads `ACTS_NATS_URL` (default `nats://127.0.0.1:4222`) and probes the connect once per test binary behind a 2 s timeout (an unreachable server is cached, so the suite costs one probe, not one per case), prints a single `skip:` line and returns early — `store()` returns `Option<Arc<Store>>` (`None` = backend unreachable) and each generated test propagates it with `let Some(store) = store().await else { return; }`; the CI workflow now fails the NATS step when the server never becomes ready, so the skip cannot silently hide a missing service
- test(bench): the act benchmark times a fixed batch of completions per criterion iteration — the timed batch was criterion's calibrated `iters`, so the untimed arming (a `proc.start()` plus an `acts.core.irq` message and a retained `(pid, tid)` per completion) and the number of live processes were whatever the calibrator picked for the machine and the warm-up/measurement targets, and the wait for the batch had no upper bound (a batch that never reported `Created`, e.g. the zero-length calibration, blocked the sample forever); `ACT_BATCH_SIZE` now fixes the batch at 8 and drives `Throughput::Elements`, and the arming is extracted into `arm_batch` (deploy and every `Created` stay outside the timer, the returned pairs are truncated to the batch) behind a 10 s `ACT_ARM_TIMEOUT` that fails the benchmark instead of hanging — verified with `cargo bench --bench workflow -- act --warm-up-time 1 --measurement-time 2`: 165 iterations armed 1320 completions, and the reported `1.0479 ms`/iter and `7.63 Kelem/s` match 8 tasks per iteration
- test(bench): the criterion suite covers the hotspots it was missing — `benches/store.rs` gains `store_eq_scan` (a selective `Eq` on the indexed `mid`, the same predicate on the non-indexed `name` as the full-scan fallback, and the id-only `matching_ids` read, at 100 and 1000 rows, which shows the index scan still costs the whole field region); a new `benches/engine.rs` covers the runtime — `expr_eval` times an `acts.core.irq` act's `${{ ... }}` params at 1/16/64 expressions per process (the expression environment is crate-private, so the start→armed span is measured end to end and the slope is the per-expression cost of QuickJS context reuse plus module refresh), `scheduler_multi_pid` starts a fixed 32-process batch per criterion iteration at 1 and 4 `scheduler_workers` lanes, and `emitter_fanout` completes a fixed 8-task batch with 1/16/64 registered channel handlers, waiting for every delivery so the per-message fan-out is inside the timed region; a new `benches/model.rs` covers `ActSchema::validate` against a cached validator (1/8/32 properties) and against a never-seen schema that recompiles the JSON Schema every iteration
- test(bench): a latency stress target reports p50/p95/p99, which criterion cannot — it prints only the mean estimate (its percentile API is internal, and `estimates.json` stops at the median), so the tail a latency budget is stated against had no output anywhere in the suite. `benches/latency.rs` (`cargo bench -p acts --bench latency`, ~10 s) measures the same hotspots as the criterion benches but keeps every individual operation's duration and prints nearest-rank percentiles — `p` is the sample at rank `ceil(p/100 * n)`, so `p99` of 200 samples is the second-largest — plus max and mean; each scenario warms up untimed and the engine scenarios size the process cache to the batch they arm (under the default `cache_cap` of 1024 an oversized armed set is *parked*: never started, so it emits nothing and the sample would time out). Scenarios: indexed vs full-scan `Eq` queries (100/1000 rows), cached vs freshly compiled `ActSchema` validation, `${{ ... }}` expression evaluation per process, a burst of 32 concurrent process starts at 1 and 4 `scheduler_workers` lanes (all 32 callers submit at once, so the spread is the queueing the lanes impose), and `act().complete()` with 1 vs 16 registered channel handlers. Verified on this machine: `store/eq_index/rows=1000` p50 48.9 µs / p99 137.8 µs against `store/eq_scan/rows=1000` p50 4.50 ms / p99 14.66 ms (n=200) — the index still reads a value-proportional region; `schema/validate_cached/fields=32` p50 20.6 µs / p99 65.1 µs (n=5000) against `schema/compile/fields=8` p50 73.1 µs / p99 161.0 µs (n=500); `engine/expr_eval/exprs=16` p50 7.05 ms / p99 10.39 ms (n=200); `engine/act_complete` p50 662 µs / p99 1.43 ms with 1 handler vs p50 881 µs / p99 1.72 ms with 16 (both n=1000, same armed batch so the two are comparable); `engine/proc_start` p50 99.8 ms / p99 121.2 ms at 1 lane vs p50 102.0 ms / p99 195.9 ms at 4 (n=768) — for this workload the burst cost is dominated by a path outside the lanes and the lane count is not distinguishable from run-to-run variance, matching the criterion `scheduler_multi_pid` bench (103.7 ms at 1 lane vs 88.0 ms at 4)
- feat(acl): an optional `[acl]` config section enforces role-based access control on every transport — the section's presence turns enforcement on (so a request with no token, or an unknown one, is refused unless `default_role` names a role), a request's token selects a role, and the role's `allow`/`deny` action-name globs decide with `deny` winning; tokens are compared as SHA-256 digests (`sha256:<hex>` keeps the clear text out of the config, a bare token is hashed at load) and a `token` shorthand grants one unrestricted role (the `requirepass` equivalent). A policy that cannot be enforced fails startup instead of silently allowing or refusing: an uncompilable pattern, one token assigned to two roles, a `default_role` naming no role, or an enabled section where no role declares any token. The credential is `authorization: Bearer <token>` metadata for gRPC (actions and the `on_message` subscription alike), an `authorization` header for HTTP (`/health` stays open for probes), and the `token` field of the action body for NATS — the broker authenticates a connection, not a request, so NATS has nowhere else to carry a per-request identity. `acts::actions::apply_as(engine, &Principal, name, options)` is the authenticated entry point and the transports call it; new error kinds `Unauthenticated`/`Denied` map to the transport's own status (gRPC `UNAUTHENTICATED`/`PERMISSION_DENIED`, HTTP 401/403, a NATS `err` reply)
- feat(acl): a role's `snapshot` table narrows which scopes of which targets its subject owns, and that ownership is re-checked at seal time — the data plane is addressed by `target` × `scope`, so a role's permission over a snapshot cannot stop at the action: `snap:get`/`snap:upsert`/`snap:remove` refuse a scope outside the caller's set, `snap:ls` answers only the scopes the subject owns (filtered, not a whole-call refusal — one tenant cannot enumerate another's), and `$subject` in a pattern expands to the authenticated subject so `["$subject"]` means "my own scope only". Because the scope key is only resolvable on the task chain, the decisive check runs at each task prepare: a process started through an action carries its caller's scope policy, sealed under a private (`__`) env key the workflow's `$env` proxy refuses to read or write, and the scheduler refuses to freeze a value the process owner does not own — a workflow can no longer read another subject's sealed data by being started with someone else's `uid`, which the action-level check alone cannot prevent. The credential is persisted with the process env, so a resumed process keeps the scope it started with; a process started in-process or by a trigger carries none and stays unrestricted, as do process rows written before this field existed
- feat(acts-cli): `--token` (or `ACTS_TOKEN`, which keeps the credential out of the process list) is presented on every request, and the CLI resolves its identity through the new `acl:whoami` action before entering the REPL — a missing or stale token now fails at startup with a hint instead of surfacing on the first command; `ActsChannel::connect_with_token` carries the token through a tonic interceptor, which covers the unary actions and the `on_message` stream in one place so no call path can silently omit the credential (`connect` stays the tokenless form)
- BREAKING: `ActError` gains the `Unauthenticated` and `Denied` variants, so an exhaustive match over it needs the new arms — they carry an ACL refusal distinctly from a payload or store failure, and `acts::actions::Error` mirrors them so each transport can answer "no credential" and "wrong credential" differently
- BREAKING: with an `[acl]` section present, `acts::actions::apply` no longer runs unconditionally — it now resolves the anonymous principal (`default_role`, or a refusal when none is configured), so an in-process embedder that never carries a token cannot sidestep the checks the transports enforce; without the section nothing is enforced and behaviour is unchanged. `apply_as` is the authenticated form, and `Runtime::start` now removes the caller's scope authority from its own `options` before the vars reach the workflow, so the credential never appears in user vars or model inputs
- feat(acts-plugin-web): every route now runs its operation through the shared action table as the request's principal — `model`/`proc`/`pack`/`snap`/`msg:ack` and the trigger hook previously reached the executor directly, which both bypassed any action-level rule and left HTTP with a second, diverging definition of each operation; a `middleware::from_fn` resolves the bearer token into a principal before any handler runs, the parsed body is re-expressed as the same action payload the gRPC and NATS transports send, a refusal answers 401 or 403 by kind, and `WebPlugin`'s middlewares protect every data route while `/health` stays open
- fix(acts-plugin-web): the trigger hook route never matched — `/hooks/{event_id}` was registered with axum 0.8 path syntax on axum 0.7, where `{param}` is a literal path segment, so every `POST /hooks/{model-id}:{trigger-id}` answered 404 and the endpoint was unreachable; the route now uses `:event_id` and reaches `evt:start` (as a consequence of the dispatch change above, over HTTP a trigger is now subject to the same ACL as any other action)
- fix(acts-plugin-nats): the live suite no longer points several engines at one subject prefix — `snapshot_actions_over_nats`, `engine_events_forwarded_to_nats` and `malformed_action_data_rejected` all configured `subject = "acts"` and published to `acts.cmd`, so with the broker shared and the tests running in parallel a request was answered by whichever engine won the race and the assertions ran against another test's engine (reproduced on the pre-change tree: 3 of 4 runs failed `snapshot_actions_over_nats`); each test now owns a distinct prefix and the ACL case adds `acts-acl-test`, restoring the suite to 6 consecutive clean runs
- doc: an access-control chapter (Chinese and English, both in the SUMMARY) covering the config format, the action-name groups, the snapshot scope rule and its seal-time enforcement, the per-transport credential, and the client flags; `acts-server`'s embedded default config gains a commented `[acl]` template and the server, web-plugin and CLI READMEs document the credential they accept
- feat(acl): directory control — `workdir` on `[acl]` (or per role to override it) confines every process the policy starts to its own directory `<workdir>/<pid>`: the directory is created at start, the process id becomes a path segment (so one that is not a single safe component — empty, `.`, `..`, or containing a path separator or colon — is refused rather than placing the run outside the root it was given, which matters because an external pid is otherwise free-form), and acts read it through the new `Context::workdir()`. The path travels the same private route as the scope authority: sealed into the process env under a key the workflow's `$env` proxy refuses to read or write, persisted with the process (a resumed run keeps its directory), and popped from the start options so the workflow never sees it. Without `workdir` there is no directory control, which is the previous behaviour
- feat(acts-package-shell): `acts.app.shell` honors the process workdir — the script runs with that directory as its working directory and with `HOME`/`TMPDIR`/`TEMP`/`TMP`/`PWD` pointing inside it, and `ACTS_WORKDIR` names it for the script, so tools that default to a home or temp directory stay inside too; a script naming an absolute path (`/etc/passwd`, `C:\Windows\win.ini`, `\\server\share`) or a `..` segment is refused before it runs. That check is policy, not a sandbox and is documented as such: the containment that actually holds is the child's working directory, since a shell can spell an outside path in ways no textual check follows (`a=/etc; cat $a/passwd`, a symlink inside the workdir) — which is why the per-process directory plus an OS boundary is the recommended pairing for hostile workflows. Four unit tests pin the allowed/absolute/traversal/URL cases
- test: `a_run_is_confined_to_its_own_workdir` starts a run through `apply_as` and asserts it got `<root>/<pid>` (and that the private key never reached its inputs), `a_workdir_refuses_a_pid_that_is_not_one_directory` asserts the refused pids left nothing behind, and `env_private_keys_are_engine_only` pins that `$env` cannot read or write the owner/workdir keys — the guard both credentials rest on had no test before
- doc: the access-control chapter (both languages), `acts-server`'s embedded config template and README, and `acts-package-shell`'s README document `workdir`, what it does and does not contain, and the `Context::workdir()` accessor
- fix: fix the typo for `env::Environment` and `env/module`
- fix: values injected into `${{ }}` expressions are no longer truncated to `i32` — `IntoJs for ActJsValue` converted every JSON integer with `as i32` (both the `i64` arm and the `u64` fallback), so any var, input, output or `Vars` field past `i32::MAX` silently entered the script as a wrong negative number: a millisecond timestamp `1757318400000` became `676775936`, `i32::MAX + 1` became `i32::MIN`, and `u64::MAX` became `0` — every workflow carrying an epoch timestamp, snowflake id or large counter computed on a corrupt value with no error. An `i64` now crosses as an int when it fits, as a double while a double carries it exactly (`|v| <= 2^53 - 1`), and as a BigInt beyond that; a `u64` above `i64::MAX` crosses as a BigInt (it is always wider than 2^53 - 1); a JS number that is integral and within the exact range comes back as a JSON integer, so the value `3_000_000_000` round-trips instead of degrading to `3000000000.0` (which no integer-typed field accepts). Two silent-corruption paths on the way out close with it: a non-finite JS number (`0/0`, `1e400`) is an error instead of `null`, and a BigInt in the `u64` range (`18446744073709551615`) is accepted rather than reported as out of range. BREAKING: a value past 2^53 - 1 arrives as a BigInt, so JS arithmetic that mixes it with a plain number (`${{ big + 1 }}`) throws — mixing requires an explicit `BigInt(1)`, which is the only way JS itself can add such values without losing digits; and a non-finite expression result now fails the act instead of evaluating to `null`
- test: `env_eval_large_numbers_round_trip_exactly` pins the reported cases (`3_000_000_000`, a millisecond timestamp, a snowflake id, `i64::MIN`, `u64::MAX`) through a registered user var and asserts an out-of-range value stays exact inside the expression too (`bignumbers.snowflake === 1234567890123456789n`); `env_eval_bigint_beyond_i64_keeps_u64` and `env_eval_non_finite_number_is_error` cover the two range errors, and `env_eval_bigint_outside_u64_is_error` pins the message past the widened range
- security: `${{ }}` expressions no longer share mutable JavaScript state with later evaluations — the environment pooled QuickJS contexts and, before reuse, restored only `globalThis`'s own properties from a snapshot, so every other write an expression made survived into the next one: `Array.prototype.__acts_secret = x` (likewise `Object.prototype`, a namespace object such as `Math`, a module global such as `$get`, a `console` property, a `$env` proxy target, or a non-configurable `globalThis` property) was still readable there — and the pooled path re-ran no module `init`, so a reused context also kept the previous process's step globals (`StepModule` builds its `Proxy` globals from `get_steps()` in `init` and has no `refresh`). With `[acl]` enabled and `secrets`/sealed data injected as expression globals, the next task — another tenant's code act — could read it. `Environment::eval` now builds its own QuickJS runtime and context per evaluation and discards both, so no realm state can cross evaluations; the runtime pool went with the context pool because rquickjs caches class and callable prototypes on the runtime (`Opaque`), which kept one realm's `Object.prototype` reachable from the next context of the same runtime, so a fresh context alone was not enough. Cost: a fresh realm and one full module init per expression, roughly 3× the pooled evaluation (measured in one release run: 1.26 ms vs 0.38 ms, of which the realm is ≈0.52 ms, module init ≈0.70 ms and the expression itself ≈0.04 ms), so `benches/engine.rs::expr_eval` now measures a per-expression realm
- test: `env_eval_isolates_realm_state` runs one evaluation that pollutes `Array.prototype`, `Object.prototype`, `Math`, `$get`, `console`, the `$env` proxy target, and a configurable and a non-configurable global, then asserts the next evaluation sees none of it; it fails against the pooled implementation (which leaked every one) and passes against the per-evaluation realm
- fix: a launch whose first durable write fails gives its pid back instead of poisoning an externally supplied pid forever — `Cache::admit` claims the pid before any store I/O and `Runtime::launch` evicted only the in-memory instance when `Process::start` failed, so a transient store fault on that first `upsert_proc_with_task` left the pid claimed with neither a resident instance nor a durable row; nothing else ever released it (the sweeper only removes rows that exist), and every later start of that pid died in admission with `proc_id(X) is duplicated in running process list` although nothing ran and no row existed for X — pointing troubleshooting at a phantom duplicate instead of the store fault that actually happened. `Cache::abandon` — the rollback `launch` now runs — drops the instance and releases the claim, keeping it only while the store still holds a row for the pid (a row occupies its pid for the row's whole lifetime and the `remove()` that ends it is what releases the claim); a store that cannot answer the row check keeps the claim too, since a row that cannot be ruled out must never be handed to a second admission
- test: `failed_launch_releases_pid_claim` injects a store that fails the first `batch` carrying a proc row and asserts that the retry with the same external pid is admitted rather than rejected as a duplicate (it fails against the `evict`-only rollback); `abandon_keeps_the_claim_of_a_durable_row` pins the complementary branch — a row that survived the failed start keeps the pid claimed until `remove()`
- fix: a process workdir no longer outlives its process — `prepare_workdir` created `<acl workdir root>/<pid>` at every start while nothing ever removed it, so a long-running server accumulated one directory (and whatever a run left in it) per historical process for the life of the deployment; the directory now follows the process's durable rows: the sweeper that deletes a settled process's rows removes its workdir in the same step (read from the process env before the row goes), and a start that never became durable — a failed launch or a parked write that errored — removes its own. The removal happens before the rows, so a crash in between leaves a row the next sweep converges on instead of a directory with no row left to find it; a row that names something other than the pid's own directory is refused, so a malformed row cannot delete an unrelated path
- feat(acl): the workdir root travels in the sealed owner authority instead of a start option — `PROC_WORKDIR_ROOT` is gone from the start options (`Runtime::start` no longer pops it), and `ScopePolicy` carries the root as `workdir_root`, so the directory a run is confined to comes only from the compiled config (`[acl] workdir`, or the role's override) and a caller of `proc:start`/`proc:start_from_model` (or an in-process `Runtime::start`) can no longer name it. The config value is a root; the process's own directory is `<root>/<pid>`, which is what `Process::workdir`/`Context::workdir` answer. The role-level override and every existing config keep working
- feat: `$env.WORK_DIR` names the directory a process runs in — the same `<root>/<pid>` as `Context::workdir()` (not the configured root), readable by a workflow script and answered from the process itself rather than stored (so a resumed run and a `Context::get_env` caller see the same value, and the private key it lives under stays out of reach); it is read-only: `Context::set_env` drops a write to that name and `Context::get_env` never falls back to a stored var or an OS variable of the same name, so a script cannot redefine where it runs; a process the policy gave no directory reads `null`
- test: `env_work_dir_names_the_process_directory` pins the readable name, its absence without a workdir, the read-only write path (script and engine-side), and that the reserved name is never stored in the process env; `a_run_is_confined_to_its_own_workdir` now runs a script that returns `$env.WORK_DIR` (asserting the run's public name reaches the run's script) and drives the sweeper until the directory is gone; `failed_launch_releases_pid_claim` asserts a start that never became durable leaves no workdir behind, and `abandon_keeps_the_claim_of_a_durable_row` asserts a durable row keeps it until the sweep removes both
- doc: the access-control chapter (both languages), `acts-server`'s embedded config template and README, and `acts-package-shell`'s README document `$env.WORK_DIR` and the directory's lifetime (created at start, removed with the process's rows)
- BREAKING: an engine whose config has **no `[acl]` section is no longer unrestricted** — it is anonymous and read-only. Every request resolves to the built-in `anonymous` subject, whose grant is exactly the read group minus the snapshot actions (`model:ls`/`get`, `proc:*`… in list/get form, `task:*`, `msg:ls`/`get`, `evt:*`, `pack:*`): no writes (`model:deploy`, `pack:publish`, `snap:*`), no control (`proc:start*`, `act:*`, `evt:start`, `msg:ack`), no admin (`*:rm`, `msg:clear`, `msg:redo`, `msg:unsub`), no snapshot scope (a scope has an owner only when a policy names one) and no subscription. `Acl::anonymous_access()` is that policy, `Acl`'s `Default` is it too, and `Engine` logs a warning naming the two ways out: add `[acl]` (a single `token` is the smallest useful section) or write `enabled = false` inside it — the explicit opt-out (now the only way to reach the pre-ACL behaviour, `Acl::disabled`) — so a deployment that never configured access control can no longer be written to by anyone who can reach it
- feat(acl): opening a message stream is an action — `msg:sub`. gRPC `on_message` and SSE `/msg/sse` run it through the same action table as every other operation (a role without it answers `PERMISSION_DENIED`/`403` instead of a stream), and the transport registers the channel under the key the action answers with rather than composing one itself, so the checked path and the occupied key cannot drift apart. `msg:unsub` composes the same key from the same client id, so a caller only names channels in its own namespace, and the channel key is `{subject}/{transport id}` — the subject prefix is what stops a second caller naming an id another subject already used from taking over that subject's handler (the emitter registry replaces a handler registered under an existing key). A role name containing `/` is refused at config load, which keeps the prefix unambiguous
- fix(acl): the message face is authorized by grants, not by the process's owner. The previous cut scoped delivery to the emitting process's sealed owner (a subject saw only its own runs' messages) and checked `msg:ack` against that owner: both are gone, so a subscriber receives every message its self-declared `type`/`state`/`uses`/`options` filters match and any holder of `msg:ack` may ack any delivery id — the same rule as every other action, and the reason `msg:ack` should be granted the way a write is granted. `ScopePolicy::sees_messages_of`, `Principal::check_message_owner`, `ChannelOptions::scope`, the delivery-owner lookup and `Runtime::proc_owner_scope` are removed with it, and a delivery no longer pays a process lookup per message
- test: `acl::tests::a_missing_section_is_anonymous_read_only` pins the new default (the read group passes, writes/control/admin/snapshot/subscribe are refused); `subscribing_needs_the_grant` and `an_ack_without_the_grant_is_refused` pin the two grant checks; `an_ack_follows_the_grant_not_the_process_owner` asserts a same-granted second caller may ack, and `export_channel_namespace_keeps_subscribers_apart` asserts two channels under one client id neither replace each other nor withhold either run; `test_subscription_needs_the_grant` / `sse_subscription_needs_the_grant` drive the refusal through both transports. The transport and dispatch suites that used an unconfigured engine now build `enabled = false` explicitly, so each case still tests its own surface rather than the default
- doc: the access-control chapter (both languages) leads with the anonymous read-only default and its two opt-outs, lists `msg:sub` in the action table, replaces the owner-based message section with the grant-based one, and adds a "what is enforced where" table (action permission / snapshot scope / channel namespace) that also names the two entry points the action table does not cover — an in-process `executor()` start and a trigger, neither of which carries caller authority. `acts-server`'s embedded config template and README, the repo `acts.toml`, and the web plugin README document the default and how to leave it
- BREAKING: a subscribed channel's id is now `{subject}/{transport id}` rather than the bare client id (SSE keeps its `acts-flow-client-` segment behind the subject prefix), which an embedder building the key by hand must mirror — `ChannelOptions::subscription_id(subject, client_id)` is the composition, and `msg:unsub` applies the same one. `ChannelOptions` itself is unchanged
- fix: a dispatch that cannot run is no longer reported as accepted — `Queue::send`/`send_next` resolved the item's process lease with `let Some(proc) = task.proc() else { return Ok(()) }`, so a task whose process was already deallocated (`Task` holds it `Weak`ly by design) was silently dropped while the caller read a success: `recover_actions`' `Exec`-overflow arm marks the durable outbox record `Dispatched` on `Ok`, and the periodic overflow consumer skips `Dispatched` rows, so such a record stopped being handed back to memory for the rest of the engine's run. Both producers now fail with `ActError::Runtime` naming the task (`cannot dispatch task '<pid>:<tid>': its process was deallocated`), and the exec-overflow arm matches the result explicitly — `Ok` marks dispatched, `QueueFull` retains the descriptor, any other error is returned instead of being swallowed — the same contract the `next` arm one branch below already had
- test: `queue_refuses_a_task_whose_process_is_gone` drops the last strong reference to a process, asserts the task's lease is gone, and asserts `send`/`send_next` both fail with a runtime error (not `QueueFull`) while the queue depth stays 0
- doc: the store's document locks are documented as what they are — a per-document serialization whose scope ends at the process boundary. `DOC_LOCKS` now names both edges: the registry is a process-wide `static`, so every engine and store handle in one process shares it (over-serialization where the databases behind equal keys are unrelated, and the reason two engines over one database in a single process need no further coordination), and there is no cross-process mutex, so two processes writing one database still race the index-row read-modify-write — a multi-writer deployment (several servers over one postgres/redis/nats database, or a second process on the same sqlite/sled file) needs coordination the engine does not provide: one writer per database, a backend conditional write, or a backend lock held across the read, since `batch` makes a single write atomic and not a read against another process's write. The same contract is stated on the public `DbCollection` trait (what an out-of-tree backend implementer reads), in the `[db]` comment of both `acts.toml` templates (the repo override and the one embedded in `acts-server`), in the storage section of the `acts-server` README, and in the installation chapters of both books
- BREAKING: `Engine::executor` now takes the caller — `executor(&principal)` — and **every** operation of the returned executor is checked against that principal before it runs. Until now only the transports were: `model:deploy`, `proc:start`, `pack:publish` and the rest reached the runtime unchecked for an embedder, so an in-process caller could do what a tokenless request could not. Each operation owns its action name once (`model_executor::DEPLOY`, `process_executor::START`, …), so the dispatch table and the executor match the same names and cannot drift apart; the action check is repeated at both entry points, which is what makes a direct `executor()` call as safe as a transported one. `Executor` also carries the principal into what it seals: `proc().start()`/`proc().start_as_owner()` and `evt().start()` put the caller's `ScopePolicy` (snapshot scopes, workdir root) into the run they start, so `acts::actions::apply_as` no longer injects it and `Runtime::start` still pops it before the vars reach the workflow
- BREAKING: `Extender` is gone; its two operations are `Executor::ext()` — `ext().register_var(&module)` (now `Result`, it is a checked operation: `ext:register_var`) and `ext().register_package(&def)` (checked as `pack:publish`, the permission the same write already needed through `pack().publish()`); `engine.extender()` becomes `engine.executor(&principal).ext()`. The engine's own registrations (the built-in packages published while starting, a live run's subflow) run on a new crate-private `Executor::engine(&runtime)`, whose identity is `Principal::unrestricted` under the `system` subject — the same code path, a different caller
- BREAKING: an unclaimed run reads nothing. `ScopePolicy::default()` was `unrestricted`, which handed the whole data plane to every process nobody's authority was sealed into (a `schedule` trigger, an embedder calling `Runtime::start`, a row written before the field existed); it is now `deny_all()`, so such a run starts, executes every task that needs no owned data, and fails at the seal of one that does, naming the subject it lacks. A policy comes from one place — the principal whose executor started the run — and a subflow inherits its parent's (`ProcessExecutor::start_as_owner`), so a child can never read more than the run that opened it. `Principal::unrestricted()` (a disabled ACL, an `allow = ["*"]` role, the `token` shorthand) is still where an unrestricted policy comes from, and it is always written at the call site
- BREAKING: the anonymous grant is the catalogue, and only the catalogue — `model:ls`, `model:get`, `pack:ls`, `pack:get`. An engine with no `[acl]` section no longer answers `proc:*`, `task:*`, `msg:ls`/`msg:get` and `evt:*`: a process row names who ran what, a delivery names who was meant to receive it, a trigger names the model it will start, and a caller the engine cannot identify is not the one those rows are about. The list is one constant (`ANONYMOUS_ALLOW`), pinned exactly by `acl::tests::a_missing_section_is_anonymous_catalogue_only`
- feat: `EngineBuilder::disable_acl()` is the test/local-demo setting for the opt-out — it writes `enabled = false` into the config, so "nothing is enforced" is spelled at the call site exactly like the config file spells it. There is no implicit one anywhere: neither a missing section (catalogue-only) nor an unclaimed process (reads nothing) is unrestricted
- feat: a trigger carries its firing caller's authority. `evt:start` was documented as an entry point that carries no caller authority, so a snapshot-backed model fired by a trigger kept the reading authority of whoever deployed it; the run it starts now carries the firing principal's `ScopePolicy`, like `proc:start` does. A `schedule` trigger has no caller and is the deny-all case of the entry above
- feat(acts-package-shell): `[shell] allow`/`deny` gate what a workflow's script may be — two glob lists over the whole script text (`*` spans `/` and newlines, `?` one character, `[abc]` a set), `deny` winning over `allow`, an empty pair meaning no restriction, and a pattern that does not compile failing startup rather than silently governing nothing. A script the policy refuses fails the act *before* anything is spawned (`ScriptPolicy::allows`, compiled once in `ShellPackage::from_config`/`new`); like the workdir check it is policy rather than a sandbox — a glob cannot see what a script will do (`a=rm; $a -rf /`) — and both books, the server's config template and the package README say so
- test: `export::executor::tests::a_limited_principal_is_refused_every_ungranted_operation` drives all 28 operations of a reader role through one executor and asserts each is `ActError::Denied` (`model:deploy` and `proc:start` included), `a_granted_operation_still_runs` pins that the refusals are the policy and not a blanket failure, `the_anonymous_callers_executor_reads_only_the_catalogue` does the same for an engine with no `[acl]`, and `a_started_run_carries_the_callers_authority` starts a run whose options forge `__owner` for another subject and asserts the run's sealed policy is still the caller's. `acts-package-shell/tests/policy.rs` runs three real workflows through an engine: a denied script fails and the file it would have written does not exist, an admitted one writes it, and a script in both lists fails — the shared engine and the allowed control case are what make the refusals mean something
- test/doc: `export_channel_namespace_keeps_subscribers_apart` now starts its two runs through `engine.executor(&principal)` (an engine with two roles, one token each) instead of hand-sealing `PROC_OWNER` into `Runtime::start`, so the sealing path under test is the one production uses; the access-control chapters (both languages) lead with the catalogue-only default and its two opt-outs, add an "executor" section (what a principal is for, what `engine.anonymous()` and `Principal::unrestricted()` mean, `disable_acl`), replace the "carries no caller authority and stays unrestricted" paragraph with the inheritance and deny-all rules, add the `ext:*` row and the directory row to the enforced-where table, and document `[shell]`; `acts-server`'s template and README, the repo `acts.toml`, the web-plugin README and the shell README follow
- ci: split the tests so each package runs separately
- BREAKING: fefactoring `KvStore` trait - rename `get` to `one` to get one key data, rename `mget` to `many` to get multi-keys data
- perf(store): split the store writer into pid-hashed shards — the single global write queue and consumer (every process's writes, task rows, outbox records and removals, applied one at a time behind one FIFO) is now `store_writer_workers` (default: available parallelism) queues each with its own consumer, and an op is routed to the shard of its pid with the same lane mapping the scheduler lanes and the event gate use; a pid's ops therefore stay on one shard and keep their enqueue order (task state durable before the outbox records that depend on it, removal after every pending write of the process), while independent processes are persisted concurrently instead of queueing behind one slow write, and `flush()` queues a barrier behind every shard so it still acks only when the whole backlog is applied
- feat(store): the sharded write path is bounded and refuses work instead of accumulating it — each queue holds `store_writer_queue_cap / store_writer_workers` ops (default 16384 total, at least one per shard), and a store that cannot keep up now stops taking *new* work: admitting a task's state write (or the `Exec` overflow record standing in for it) returns `ActError::QueueFull` with nothing queued and nothing awaited, which is what the scheduler's `push` already turns into its durable outbox path, and the writer latches saturated (releasing only once the backlog is back under half the capacity) so new work is refused even when the target shard has room — `Runtime::store_writer_saturated()` / `Cache::store_writer_saturated()` expose it, and both transitions are logged. The bookkeeping of work already in flight (task state transitions, `next`/action outbox records and their closes, delivery closes, removals) is never dropped: those ops wait for room, stalling the process they belong to instead of losing its durability
- fix: change `TaskState::is_success` to `TaskState::is_biz_success` to avoid confusion with the state names `cancel`, `removed`, `backed` and `submitted`
- fix(acts-server): the hourly log files are pruned instead of accumulating forever — `init_log` opened its appender with `tracing_appender::rolling::hourly(dir, "acts.log")`, which has no retention, so every hour the server ran left a new `acts.log.<YYYY-MM-DD-HH>` under `[log].dir` (by default `~/.acts/log`) and nothing ever removed one; the appender is now built through `RollingFileAppender::builder().rotation(HOURLY).filename_prefix("acts.log").max_log_files(n)`, so the oldest file is deleted at every rotation and stale files are pruned once at startup — a restart reclaims the space as well, which the rotation-only limit of a plain `max_files` would not. `[log].max_files` is the bound: 168 hourly files by default (one week), `0` keeps every file, and the floor is 2, because the appender prunes the existing files down to `max_files - 1` before it opens the next one — `max_files = 1` would remove the file the server is still writing to (and log a failed removal per rotation on Windows). `ConfigLog` and `DEFAULT_LOG_MAX_FILES` are re-exported from `acts` (`Config::log()` already returned the type, which was not nameable outside the crate) and `acts-server::log_file_appender()` is the shared constructor the binary and the tests use
- test: `config::tests::log_file_retention_defaults_and_parses_max_files` pins the default, `0`, the floor and that a `[log]` section written before `max_files` existed still parses (the field is `#[serde(default)]`, so no existing config stops loading); `acts-server`'s `log_file_appender_prunes_stale_hourly_files` builds the real appender over 10 stale hourly files and asserts the limit (3 kept: `max_files - 1` plus the current hour's file) and the opt-out (0 keeps all 11). Verified end to end against the built binary with a temp `ACTS_CONFIG_DIR`: 5 stale `acts.log.2020-01-01-0{1..5}` plus `max_files = 3` leaves `-04`, `-05` and the current hour, and `max_files = 0` leaves all 10 plus it
- doc: `acts-server`'s embedded config template, the repo `acts.toml` and the acts-server README document `[log].max_files` — what it bounds, the 168 default, `0`, the 2-file floor, and that stale files are pruned at startup too
- build(acts-server): `tracing-appender` asks for `0.2.5` — `max_log_files` is 0.2.3+, while the manifest still pinned `0.2.2` and the resolver happened to pick 0.2.5 from the lock file
- fix(acts-plugin-web, acts-plugin-grpc): a subscriber that stops reading is disconnected instead of accumulating a task per message. Both transports wrote into a bounded queue (SSE 100, gRPC 128) and then, for *every* message, spawned a task to `send(msg).await` on it — so the bound never applied to anything: each message past the queue left a task holding that message and its context, alive until the client read, which a client that stopped reading never does. A stalled subscriber (or several) therefore grew tasks and memory without limit and put the whole runtime under a scheduler/allocator burden from a handshake any `msg:sub`-granted role can open — with `enabled = false` or no `[acl]` section, anyone. The handlers now use `try_send`: the queue is the subscription's only backlog, and a message that does not fit means the client stopped reading, so the subscription is closed right there (`Channel::close()`, the same deregistration the drop guard performs) and the client is told by its stream ending rather than by a silent drop. What the engine still owes that channel is not dropped with the client: a delivery handed over but not acked — of a process that has not settled, since a settled task's deliveries are closed with it — is re-sent by the retry timer to the channel the client occupies again (a resubscribe composes the same channel key, the action's `{subject}/{client id}`, so the backlog arrives at the new stream), which is what the delivery re-arm path already did for any disconnect. The queue no longer holds a per-message successor task at all: the dispatch path writes into it directly
- feat(acts-plugin-web, acts-plugin-grpc): the per-subscriber queue is configurable — `[web].queue_size` (default 100) and `[grpc].queue_size` (default 128), clamped to `1..=65_536` so neither a zero (a subscription that could never receive a message) nor an absurd backlog is configurable. The plugins re-export `DEFAULT_QUEUE_SIZE`, like the engine does for its own defaults
- test: `sse_slow_subscriber_is_disconnected_instead_of_queueing` and `test_on_message_slow_subscriber_is_disconnected` open a subscription with `queue_size = 2`, never poll its body/stream, drive two runs past the bound, and assert that the stream ends (the reader is not left queued) and that no further delivery row is stored for the closed channel, and the SSE case resubscribes under the same client id and asserts the unacked deliveries come back — both hang on the spawn-per-message handler; `test_http_config_queue_size_is_honored_and_clamped` / `test_grpc_config_queue_size_is_honored_and_clamped` pin the configured capacity, the default and the `0` floor
- doc: the access-control chapters (both languages) document the bounded backlog and the disconnect, and `acts.toml`, `acts-server`'s embedded template and README, and both plugin READMEs document `queue_size`
- fix(acts-plugin-nats): a busy actions subject no longer turns into unbounded work. `serve_actions` answered every message on `<subject>.cmd` by spawning a task of its own the moment the message arrived, with nothing bounding how many of them existed: a publisher that kept the subject busy — a retry loop, a bug, or a hostile client, and it needed no grant at all, since the task was spawned before the action was authorized — grew tasks and the deploys, process starts, store writes and outbound calls they run for as long as it kept publishing. An action is now admitted through `ActionsInFlight` (a permit is held for the whole action, so everything it does happens under it) and one that cannot be admitted is refused to its caller instead of being started: the reply is `err: "too many actions in flight (N); retry later"`, echoed with the caller's own `name`/`seq` (the payload is parsed before admission, which is also why a malformed one is still answered rather than refused). Nothing is dropped silently, and the refused path costs a parse and a reply — no task, no engine entry. The overload is latched the way the store writer's saturation is (one line on entering the bound, one on leaving it) instead of a log line per refused message, which would have made the flood's cost a log line per message
- feat(acts-plugin-nats): `[nats].max_in_flight` is that bound (default 256; `0` selects the default, like the engine's own queue caps), clamped to `1..=65_536` so neither a zero-capacity nor an absurd bound is configurable. `acts_plugin_nats` re-exports `DEFAULT_MAX_IN_FLIGHT`
- test: `actions_beyond_the_in_flight_bound_are_refused` publishes a 256-request burst to one reply inbox with `max_in_flight = 1` and asserts that every request is answered (refusals included, so a caller never hangs or has to guess whether its action ran), that the burst is refused past the bound, and that an admitted action still runs — with one permit and a store read taking far longer than the loop needs per message, 246 of the 256 are refused and 10 run. Against the spawn-per-message handler it fails with `0 of 256 refused`. `actions_are_admitted_only_under_the_bound` pins the admission itself (a finished action frees its slot; the refusal names the bound), and `max_in_flight_is_configured_defaulted_and_clamped` pins the configured value, the default and the `0`/absurd floors. `cargo test -p acts-plugin-nats` 6 + 5 passed against a live broker
- fix(acts-plugin-nats tests): the live suite's temporary config no longer collides — its file name carried `SystemTime::now().as_nanos()`, which two tests running in parallel threads can land on within the clock's resolution, and the loser read a half-written file and panicked in `Config::create` (seen as an unrelated failure of `engine_events_forwarded_to_nats` once the burst case joined the file). The name is now the process id plus a counter
- doc: the NATS plugin README, the repo `acts.toml` and `acts-server`'s embedded config template and README document `[nats].max_in_flight` — what it bounds, the default, the refusal a caller receives, and why a task per message was unbounded work
- build: one version per dependency again — every version that two or more members declare now lives in `[workspace.dependencies]` and is inherited, and the three clients that had drifted onto two versions each are on one: `acts-store` asked for `async-nats 0.49.1` and `redis 0.27` while `acts-package-nats`/`acts-plugin-nats`/`acts-server` resolved `async-nats 0.50` and `acts-package-state` resolved `redis 0.31`, so the workspace compiled two NATS clients and two Redis clients whose TLS, retry and timeout semantics are free to differ, and a security fix for either had to be tracked on two version branches. `acts-channel` also declared `thiserror 1.0.66` without using it (nothing in that crate names `thiserror`), which kept `thiserror 1.0.69` in the graph beside the `2.0.20` that `acts` uses; the declaration is gone. Neither client needed a source change for its new version, and both were verified against a live server; `shlex` moved to 2 (the version `cc` already pulls, and `shlex::split` is unchanged), `parking_lot` is declared once instead of twice in `acts`, and the example crates inherit the workspace's `acts*` entries instead of declaring bare `path` dependencies. `cargo tree --workspace --duplicates` no longer lists `async-nats`, `redis`, `thiserror` or `shlex`; what remains is transitive and each entry is named with its reason in `deny.toml`
- build: `Cargo.lock` is committed — it was in `.gitignore`, so every CI run (and every `cargo build` that noticed a manifest change) resolved the graph afresh and a release binary was built from whatever version resolution happened that day, which is the same drift the manifests were fixed for, one layer down. The lock is now in the repository, the CI builds (`build`, `clippy`, `test`, `release`) run `--locked` so a version change that forgets to refresh the lock fails loudly instead of resolving something new, and Dependabot proposes the refresh weekly (patch/minor grouped, majors separate)
- ci: a `deny.toml` policy plus a `supply-chain` workflow that runs `cargo deny --locked check` and `cargo audit` (moved out of `rust.yml`, so the advisory policy has one home) on push, on pull request, weekly on a schedule, and on demand. `deny.toml` is what keeps this from happening again: `bans.workspace-dependencies.duplicates = "deny"` fails when two members declare the same crate without inheriting the workspace version (the rule the `redis`/`async-nats` split broke), `bans.multiple-versions = "deny"` fails on a *new* duplicate version while the existing transitive ones are listed with a reason each, and the advisories/licenses/sources checks cover the rest — the check found `rustls 0.23.44` (RUSTSEC-2026-0285), and with that version fixed the graph reports no vulnerability, no unmaintained or unsound advisory and no unapproved license; what is left to maintain is the transitive duplicates the `skip` list names with a reason each (`parking_lot 0.11` through `sled 0.34`, `tower 0.4` through `tonic 0.12`, and the `windows-sys` majors among them)
- security: `rustls` is 0.23.45 — the resolved 0.23.44 accepts TLS 1.3 handshake messages that should be encrypted in plaintext across encryption-level boundaries (RUSTSEC-2026-0285 / GHSA-2mjx-qc3c-rqvc, the same bug as Go's GO-2026-4340), reached through `reqwest`, `sqlx-core` and `async-nats`; `deny.toml`'s advisories check is what surfaced it
- fix(scheduler): the store-facing background timers back off instead of polling a store that is down — the schedule-trigger query, the unacked-delivery retry, the settled-process sweep and the durable-overflow replay each logged their error and went straight on to the next tick, so a store outage meant one query that could not succeed per tick for the whole outage, one error line per tick, and no state anywhere that said the loop was not doing its job (an idle scheduler and a failing one reported the same thing); both timers now track consecutive failed ticks, and from the third one the loop is *degraded*: it skips a doubling window of ticks (1, 2, 4, 8 — capped, so a store that comes back is still probed within 8 ticks of it answering) instead of querying every one, logs the degradation and each widening with `loop_name`/`consecutive_failures`/`backoff_ticks` fields instead of a warning per attempt, and the first clean tick clears the streak, reports the recovery and restores the tick cadence — all of it without a restart, and readable per loop as `Runtime::scheduler_health()`
- test: the backoff is pinned in `scheduler::tests::health` — `guard_degrades_widens_to_a_cap_and_recovers` walks the state machine (a streak below the threshold stays healthy, the failure that crosses it degrades and skips exactly one tick, the window doubles to the cap and stops, one clean tick clears all of it), and `trigger_loop_degrades_under_a_down_store_and_recovers` runs the real trigger timer over a KV backend whose `events` reads fail: the loop reports degraded with a non-empty window, issues strictly fewer store queries than the ticks that elapse while it is degraded (the skipped ticks are what a dead store no longer pays for), and returns to healthy on its own once the backend answers

- fix: a delivery row can no longer outlive the close that settles its task. The row was written straight into the store from the channel delivery path, while the task's terminal write (its `close_deliveries` above all) is applied on the process's writer shard — two paths with no order between them, so a message emitted as its task finished could store its delivery *after* the close had already run. The row stayed open on a task that is over, and nothing settles a delivery of a finished task afterwards: the retry timer re-sent it until its retries ran out and left it `Error`, which keeps a process alive by design, so the finished process's rows — its pid, its task rows, its workdir — could never be swept (the sweeper only deletes a process whose deliveries all settled). The create is now a shard op of the process it belongs to (`WriteOp::StoreDelivery` → `Cache::store_delivery` → `Store::store_message_delivery`), so it is applied in the order of that process's task writes and its removal, and the row's fate is decided there: the close sees the row, or the row sees a task that is already closed and is born `Completed` (the client is not asked to act on a finished task — the rule the close applies), or the removal came first and the message is handed over without a delivery row instead of re-creating rows nothing would ever sweep. Absence is not a close: a task with no durable row yet (its write was refused while the write path was saturated) and a message emitted outside any process (`Emitter::emit_message`) stay open for the client to ack
- test: `store::store::tests::a_delivery_is_born_settled_when_its_task_already_closed` walks the decision at the store — a running task's row stays `Created`, a closed task's row is born `Completed` (with the canonical message row it carries), a task without a durable row and a message without a process stay `Created`, and a removed process stores neither row — and `scheduler::tests::action::sch_a_delivery_stored_after_its_task_closed_is_born_settled` drives it through the real channel path: a straggler message of a closed task, emitted while the process is still held open by a waiting act, is stored `Completed` (without the decision it stores `Created`)
- test: `export::actions::tests::an_unsub_reaches_only_the_callers_own_namespace` compares the message ids its channel received instead of counting deliveries — the channel is an ack channel the case never acks, so the retry timer re-sends the same message (at-least-once) and the counter read a redelivery as a second message arriving, failing the case on a retry tick rather than on the unsub; the comparison still catches a broken `msg:unsub` (with the removal skipped, the run's three messages per start show up in the set)
- test: `scheduler::tests::cancel::sch_shutdown_reaches_the_running_act` waits for the probe's report instead of reading it the instant `Engine::close` returns — `close` fires the cancellation token, the act's own task is scheduled after that, so a single read raced the act's wake-up (the sibling `abort` case already waits for the same report)

# Unreleased
- feat(acts): model operations with an explicit six-phase lifecycle (`Created`, `Durable-Pending`, `Dispatched`, `Effect-In-Flight`, `Effect-Durable`, `Completed`); durable outbox rows now carry `phase` alongside the scheduler-facing status, advance forward-only, and record source/target task identity, source generation and causal operation id (`Op` schema v2 with v0/v1 upcasts)
- feat(acts): replace the implicit `NEXT_COMPLETE` sign with a durable `PropagationPhase`; task vars persist when parent propagation is pending or applied, parent completion waits for a child's applied propagation, and recovery closes an already-applied operation instead of replaying it
- fix(scheduler): mark Error and Abort propagation as applied as each affected task state is updated, making recursive ancestor updates idempotent across crash recovery; add Error/Abort operation types and queue/recovery descriptors for durable propagation handling
- test: cover operation phase ordering and migrations, applied propagation recovery, Error/Abort replay behavior and normal `next` outbox completion
- perf(acts): the write barrier a reader waits for now matches what it reads — a cache-miss load barriers only the pid's own shard (`StoreWriter::flush_pid`: every op of one pid is FIFO on one shard, so a per-shard barrier gives the load the same read-after-write visibility as the global `flush` it replaces) instead of waiting for the backlog of every shard, and the settled-process sweep queues all its `RemoveProc` ops and pays one barrier for the whole batch instead of one all-shard wait per process (up to 256 per tick). The sweep keeps its closing barrier: the same tick's overflow replay reads the store directly (bypassing the writer), so without it the replay could observe the rows and outbox records already committed for deletion and re-load a just-swept finished process into a resident slot, and the barrier is what surfaces a failed removal to the sweep's `health.failed`. `Cache::flush` becomes test-only — no runtime caller waits for the whole writer any more: a miss scopes to its shard, the sweep batches one barrier, shutdown drains through `close`
- test: `flush_pid_waits_only_for_its_own_shard` parks one pid's writes inside the store through a pid-keyed gate over the collection rows (the index keys carry the pid; the gate matches the `-`-escaped form `=2D` the key encoding stores) and asserts a `flush_pid` for a pid on another shard acks while the global `flush` cannot complete until the gate opens
- fix(acts): the outbox dedup's check-then-create is only race-free under the writer's per-pid serialization — every op of one pid applies on one shard, strictly sequentially, and the KV layer has no atomic unique insert to fall back on — and the one creation surface that lived outside that invariant is gone: `enqueue_propagation_op` had no caller, and its natural ones (a queue producer spilling a propagation on `QueueFull`, a lane worker) run off the writer shard, so wiring it would have raced the shard consumer and stored a second `Pending` record for one `(pid, tid, type)`, replayed twice on recovery. The invariant is now documented on `enqueue_op` and its three writer-facing wrappers
- test: `cache_concurrent_enqueues_leave_one_pending_record` fires 32 racing `enqueue_action` for one `(pid, tid)` through the real writer and asserts exactly one `Pending` record survives; with the dedup check disabled the same run stores 32 records and fails (`left: 32`), so the test bites on exactly the race the writer's serialization prevents
- fix(scheduler): a process that is over never grows again — the abort/error walk marks its tasks terminal while a pending `next` dispatch is still in flight for the same slot, and the task that dispatch created was unreachable: no client was ever asked for it (its message was never emitted) and its completion was refused (`cannot find process`), so it waited forever, kept a step non-terminal and wedged the run. `Context::sched_task`, `sched_task_with_vars` and `dispatch_act` now refuse to create under a process whose state is terminal, the queued `Exec` dispatch refuses to run work for one, and `schedule_once` refuses to schedule from a predecessor that is already `Aborted`/`Error`
- fix(scheduler): a terminal decision sticks — `Task::set_state` checked `is_running()` and wrote the new state under separate acquisitions of the state lock, so a completion pass that read `Running` could overwrite a client `abort`/`error` that landed in between (`set_state` is now `set_state_impl`, with `set_state_if_running` performing check and write under one lock, used by the child-driven completion in `Task::auto_complete` and `Workflow::next`). An errored or aborted run can no longer settle as a business success
- fix(scheduler): the `Error` action heals its own lost row write — the arm wrote the errored act's terminal row once and then closed the action's outbox record regardless, so a transient store fault there left the row at the stale pre-action `interrupt` and a crash + reload restored the errored act as a waiting act in a process that survives the error (a catch branch), which could then never finish. The arm re-emits the errored act after the error walk, so the durable row converges to `Error`
- fix(scheduler): a duplicate `Cancel` can no longer build a second redo path — the guard lives on the parent step, which stays `biz_success` after the first cancel, so a redelivered cancel re-applied: it created a second live redo instance and a second downstream act the client never drives, leaving an orphan `Interrupt` act and a process that never finishes. `Context::redo_task` now never creates a second instance for a `(node, prev)` slot while one is still live (the same reuse rule `schedule_once` uses for replayed `next` work), and the cancel arm refuses before any redo when the forward path it would rewind is already terminal
- fix(acts): an outbox close that reaches the writer after its process was removed is skipped instead of re-creating a row nothing would ever delete — the removal is queued on the writer (FIFO) but a close enqueued before it can be applied after it, so a finished and swept process could leave a `done` operation row behind. `Cache` records the pids whose removal it queued, and `complete_action`/`complete_next` skip that dead bookkeeping
- test(acts): the reliability matrix of `crates/acts/src/scheduler/tests/reliability/` — every feature row crossed with store failure, restart, duplicate delivery and concurrency, 58 cells split per client action (`action`, `next`, `cancel`, `abort`, `remove`, `error`, `back`, `parallel`, `subflow`, `timeout`, `timer`) with every shared helper in `mod.rs`. The cells the crate already had were moved in (the old `scheduler/tests/action.rs` was itself a reliability suite — its cells are now distributed by the action they exercise, and the file is gone, as is `scheduler/tests/cancel.rs`; `message.rs`, `health.rs`, `proc.rs` and `step/timeout.rs` kept only their happy paths)
- test(acts): every reliability cell runs under a deadline guard (`super::bounded`) — a stalled engine now fails the cell with a named message instead of freezing the suite behind the `#[serial]` lock (an intermittent stall on the restart/recovery path, present before this work, was observed freezing whole runs)
- test(acts-package-shell): `tests/reliability.rs` pins the shell package's two reliability cells — an interrupted script resumes once after an engine restart (the `Func` act re-runs across the reload, exactly one process row, the side-effect file exists), and a task-state write that fails once degrades without losing the run (the run completes exactly once, the fault surfaces on the barrier that drains its shard, no task is left running, and a later act still runs on the released lane)
- test(acts): `sch_error_store_fail_degrades_and_heals_once` pins the strong outcome — an errored run must settle as its error, never as a business success — and `sch_abort_leaves_no_live_downstream_act` (40 rounds) is the regression for the dead-run guard: before it, step2's act was created under the aborted process within the first rounds
- doc(acts): two racing cells stay `#[ignore]`d with the evidence written next to them — racing `Next` against `Abort` (and against `Back`) can admit both decisions, because their guards are check-then-apply and the walk-based actions have no atomic claim; a per-task claim/lock held across the application deadlocks against the runtime's own jobs for the same task (a waiting lock hung 3 of 3 runs, an atomic claim with a bounded spin still hung one), so the fix belongs with the runtime's job/decision interaction, not in `Task::update` alone
