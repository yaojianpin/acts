name: actions
id: actions
steps:
  - name: set state value
    id: set-value
    acts:
      - uses: acts.app.state
        params:
          op: SET
          key: key1
          value: 100
  
  - name: get state value
    id: get-value
    acts:
      - uses: acts.app.state
        params:
          op: GET
          key: key1

