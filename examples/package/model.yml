id: pack_example

options:
  exposes:
    - name: result
      value: '{{ input }}'

steps:
  - name: step 1
    id: step1
    uses: pack1
    params:
      v1: '{{ input }}'
  - name: step 2
    id: step2
    uses: pack2
    params:
      a: '{{ input }}'
      b: [ "a", "b" ]



    