target
test_data
.vscode
log
# The workspace lock is committed (that is what CI builds with `--locked`);
# the stale per-example lock files left over from standalone builds are not.
**/Cargo.lock
!/Cargo.lock
.idea
/test_data
/test
/acts/test
/acts.cfg
doc/book/
.claude
docs/
.superpowers/
data/